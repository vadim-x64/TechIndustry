# Working with Tables and Data Types

Таблиці та типи даних — це фундамент реляційних баз даних.
Правильне проєктування таблиць і вибір типів даних
напряму впливають на **продуктивність, зручність та безпеку** роботи з базою.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- створювати таблиці в PostgreSQL
- розуміти основні типи даних
- додавати, змінювати та видаляти записи
- усвідомлювати важливість правильної структури таблиць

---

## 🧠 Теорія

### Що таке таблиця
**Таблиця** — це основна структура зберігання даних у реляційній базі.

Вона складається з:
- стовпців (columns) — описують тип даних
- рядків (rows) — зберігають конкретні значення

Кожна таблиця має чітку структуру.

---

### Створення таблиці
Таблиці створюються за допомогою команди `CREATE TABLE`.

```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  age INTEGER
);
```

Тут:
- `id` — унікальний ідентифікатор
- `PRIMARY KEY` — головний ключ таблиці
- `SERIAL` — автоматичне збільшення значення

---

### Основні типи даних
PostgreSQL має багато типів даних, але найпоширеніші:

#### Числові типи
- `INTEGER`
- `BIGINT`
- `DECIMAL`

```sql
price DECIMAL(10, 2)
```

---

#### Рядкові типи
- `VARCHAR(n)`
- `TEXT`
- `CHAR(n)`

```sql
email VARCHAR(255)
```

📌 `TEXT` не має обмеження довжини.

---

#### Дата і час
- `DATE`
- `TIME`
- `TIMESTAMP`

```sql
created_at TIMESTAMP
```

---

#### Логічний тип
- `BOOLEAN`

```sql
is_active BOOLEAN
```

---

### Обмеження (constraints)
Обмеження захищають дані від помилок.

Основні:
- `NOT NULL`
- `UNIQUE`
- `PRIMARY KEY`
- `FOREIGN KEY`

```sql
email VARCHAR(255) UNIQUE NOT NULL
```

---

### Додавання даних
Для додавання записів використовується `INSERT`.

```sql
INSERT INTO users (name, age)
VALUES ('Anna', 25);
```

---

### Оновлення даних
```sql
UPDATE users
SET age = 26
WHERE name = 'Anna';
```

📌 `WHERE` дуже важливий, щоб не оновити всі записи.

---

### Видалення даних
```sql
DELETE FROM users
WHERE age < 18;
```

---

### Перегляд структури таблиці
```sql
SELECT * FROM users;
```

Також можна переглянути опис таблиці через спеціальні інструменти PostgreSQL.

---

## 💻 Простий приклад

```sql
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  price DECIMAL(8, 2),
  in_stock BOOLEAN DEFAULT true
);

INSERT INTO products (title, price)
VALUES ('Laptop', 999.99);
```

---

## 📝 Підсумки
- Таблиці — основа PostgreSQL
- Типи даних визначають формат збереження
- Constraints захищають дані
- Правильна структура = надійна база

# CRUD Operations in MongoDB

CRUD — це базові операції роботи з даними:
**Create, Read, Update, Delete**.
У MongoDB ці операції виконуються над документами в колекціях
і є основою будь-якої взаємодії з базою даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке CRUD
- виконувати базові операції в MongoDB
- працювати з колекціями та документами
- писати прості MongoDB-запити

---

## 🧠 Теорія

### CRUD у MongoDB
CRUD означає:
- **Create** — створення документів
- **Read** — читання даних
- **Update** — оновлення документів
- **Delete** — видалення даних

У MongoDB всі ці операції виконуються над **колекціями**.

---

### Create — створення документів
Для додавання даних використовується `insertOne` або `insertMany`.

```js
db.users.insertOne({
  name: "Anna",
  age: 25,
  isActive: true
});
```

Або декілька документів:

```js
db.users.insertMany([
  { name: "Oleh", age: 30 },
  { name: "Maria", age: 22 }
]);
```

📌 Якщо колекції не існує — MongoDB створить її автоматично.

---

### Read — читання даних
Для отримання документів використовується `find`.

```js
db.users.find();
```

З фільтрацією:

```js
db.users.find({ age: { $gte: 25 } });
```

---

### Find One
```js
db.users.findOne({ name: "Anna" });
```

Повертає лише один документ.

---

### Update — оновлення даних
Оновлення виконується через `updateOne` або `updateMany`.

```js
db.users.updateOne(
  { name: "Anna" },
  { $set: { age: 26 } }
);
```

📌 `$set` оновлює лише конкретні поля.

---

### Update Many
```js
db.users.updateMany(
  { isActive: true },
  { $set: { verified: true } }
);
```

---

### Delete — видалення документів
```js
db.users.deleteOne({ name: "Maria" });
```

Або кілька документів:

```js
db.users.deleteMany({ age: { $lt: 18 } });
```

---

### Фільтри та оператори
MongoDB має набір операторів для фільтрації:

- `$eq`, `$ne`
- `$gt`, `$gte`
- `$lt`, `$lte`
- `$in`

```js
db.users.find({ age: { $in: [20, 25, 30] } });
```

---

## 💻 Комплексний приклад

```js
db.orders.insertOne({
  user: "Anna",
  total: 120,
  status: "paid"
});

db.orders.find({ total: { $gt: 100 } });

db.orders.updateOne(
  { user: "Anna" },
  { $set: { status: "shipped" } }
);
```

---

## 📝 Підсумки
- CRUD — основа роботи з MongoDB
- Дані зберігаються у документах
- Фільтри дозволяють гнучко отримувати дані
- MongoDB CRUD простіший за SQL-аналоги

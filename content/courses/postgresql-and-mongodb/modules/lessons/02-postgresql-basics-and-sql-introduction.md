# PostgreSQL Basics and SQL Introduction

PostgreSQL — це потужна реляційна система керування базами даних.
Вона використовує мову **SQL** для роботи з даними та широко застосовується
у веброзробці, аналітиці та корпоративних системах.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- зрозуміти, що таке PostgreSQL
- знати, для чого використовується SQL
- виконувати прості SQL-запити

---

## 🧠 Теорія

### Що таке PostgreSQL
**PostgreSQL** — це відкрита реляційна база даних,
яка відома своєю надійністю та відповідністю стандартам SQL.

Основні переваги PostgreSQL:
- стабільність
- безпека
- підтримка складних запитів
- масштабованість

📌 PostgreSQL часто використовується у production-системах.

---

### Що таке SQL
**SQL (Structured Query Language)** — це мова для роботи з реляційними базами даних.

За допомогою SQL можна:
- отримувати дані
- додавати нові записи
- оновлювати та видаляти інформацію
- створювати таблиці

---

### Таблиці та рядки
У PostgreSQL дані зберігаються у **таблицях**.

Таблиця складається з:
- стовпців (columns)
- рядків (rows)

Приклад таблиці `users`:
```
id | name | age
```

---

### Простий SELECT
Запит `SELECT` використовується для отримання даних.

```sql
SELECT * FROM users;
```

📌 `*` означає «усі стовпці».

---

### Вибір конкретних стовпців
```sql
SELECT name, age FROM users;
```

Цей запит поверне лише потрібні поля.

---

### Фільтрація даних
```sql
SELECT * FROM users
WHERE age >= 18;
```

`WHERE` дозволяє відбирати дані за умовами.

---

### Сортування результатів
```sql
SELECT * FROM users
ORDER BY age DESC;
```

📌 `DESC` — за спаданням, `ASC` — за зростанням.

---

## 💻 Простий приклад

```sql
SELECT name
FROM users
WHERE age > 25
ORDER BY name;
```

---

## 📝 Підсумки
- PostgreSQL — реляційна база даних
- SQL — мова для роботи з таблицями
- Дані зберігаються у рядках і стовпцях
- `SELECT` — основа будь-якого запиту

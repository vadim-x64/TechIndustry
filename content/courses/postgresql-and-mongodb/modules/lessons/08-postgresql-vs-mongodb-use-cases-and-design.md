# PostgreSQL vs MongoDB: Use Cases and Design

На завершення курсу ми порівняємо PostgreSQL та MongoDB,
розглянемо **сценарії використання**, переваги та недоліки кожної бази,
а також принципи вибору правильної бази даних під конкретну задачу.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти ключові відмінності PostgreSQL і MongoDB
- обирати базу даних під конкретний проєкт
- проєктувати структуру даних для SQL та NoSQL
- уникати типових помилок при виборі БД

---

## 🧠 Теорія

### PostgreSQL і MongoDB — різні підходи
PostgreSQL і MongoDB вирішують схожі задачі,
але використовують **принципово різні підходи** до зберігання даних.

- PostgreSQL — реляційна база даних (SQL)
- MongoDB — документна база даних (NoSQL)

---

### Ключові відмінності

| PostgreSQL | MongoDB |
|-----------|---------|
| Таблиці та рядки | Колекції та документи |
| Чітка схема | Гнучка структура |
| SQL-запити | JSON-подібні запити |
| JOIN між таблицями | Вкладені документи |
| Сильна консистентність | Гнучка масштабованість |

---

### Коли обирати PostgreSQL
PostgreSQL краще підходить, якщо:

- дані мають чітку структуру
- є складні звʼязки між таблицями
- потрібні JOIN-запити
- важлива цілісність даних
- використовується фінансова або бізнес-логіка

Приклади:
- банківські системи
- CRM
- бухгалтерія
- аналітичні платформи

---

### Коли обирати MongoDB
MongoDB краще підходить, якщо:

- структура даних часто змінюється
- дані мають вкладену форму
- потрібна висока масштабованість
- важлива швидкість розробки
- працюєш з JSON / API

Приклади:
- вебзастосунки
- мобільні застосунки
- логування
- системи рекомендацій

---

### Проєктування структури даних
У PostgreSQL:
- нормалізація даних
- розділення на таблиці
- використання foreign keys

У MongoDB:
- денормалізація
- вкладені документи
- мінімізація JOIN-логіки

📌 Правильний дизайн зменшує складність і підвищує продуктивність.

---

### Часті помилки
- використання MongoDB як SQL
- надмірна денормалізація в PostgreSQL
- неправильний вибір БД «бо популярно»
- ігнорування масштабування на старті

---

### Можливість комбінування
У реальних проєктах часто використовують **обидві бази**:

- PostgreSQL — для основних бізнес-даних
- MongoDB — для логів, подій, сесій

Це нормальна та ефективна практика.

---

## 💻 Простий приклад вибору

```text
User accounts → PostgreSQL
User activity logs → MongoDB
```

Такий підхід поєднує сильні сторони обох систем.

---

## 📝 Підсумки
- PostgreSQL і MongoDB вирішують різні задачі
- Немає «кращої» бази — є правильний вибір
- Структура даних визначає технологію
- Грамотний дизайн = успішний проєкт

---

🎉 **Вітаємо! Ти завершив курс PostgreSQL & MongoDB**
# Introduction to MongoDB and NoSQL

MongoDB — це популярна NoSQL база даних, яка використовується
для зберігання **гнучких та масштабованих даних**.
Вона добре підходить для сучасних вебзастосунків, API та систем з великою кількістю змінних даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке NoSQL бази даних
- пояснити відмінності між SQL та NoSQL
- знати основні концепції MongoDB
- розуміти, коли MongoDB краще за реляційні бази

---

## 🧠 Теорія

### Що таке NoSQL
**NoSQL** — це підхід до зберігання даних,
який не використовує класичні реляційні таблиці.

Основні ідеї NoSQL:
- відсутність жорсткої схеми
- горизонтальне масштабування
- висока продуктивність
- гнучка структура даних

📌 NoSQL не означає «без SQL», а означає **Not Only SQL**.

---

### Типи NoSQL баз даних
Існує кілька основних типів NoSQL баз:

- документні
- key-value
- колоночні
- графові

MongoDB належить до **документних баз даних**.

---

### Що таке MongoDB
**MongoDB** — це документно-орієнтована база даних,
у якій дані зберігаються у форматі **JSON-подібних документів (BSON)**.

Основні переваги MongoDB:
- гнучка структура документів
- просте масштабування
- зручна робота з вкладеними даними
- популярність у web та cloud-проєктах

---

### Документи та колекції
У MongoDB:
- **документ** ≈ рядок у таблиці
- **колекція** ≈ таблиця

Приклад документа:
```json
{
  "name": "Anna",
  "age": 25,
  "skills": ["Python", "SQL"]
}
```

📌 Документи в одній колекції можуть мати різну структуру.

---

### Відсутність жорсткої схеми
На відміну від PostgreSQL, MongoDB не вимагає
попереднього опису структури даних.

Це означає:
- легше змінювати формат даних
- швидше розробляти прототипи
- більше відповідальності на логіці застосунку

---

### Ідентифікатор _id
Кожен документ у MongoDB має унікальне поле `_id`.

```json
"_id": ObjectId("...")
```

📌 `_id` створюється автоматично, якщо не заданий вручну.

---

### MongoDB vs PostgreSQL
Основні відмінності:

- PostgreSQL — таблиці, MongoDB — документи
- PostgreSQL — чітка схема, MongoDB — гнучка структура
- PostgreSQL — складні JOIN, MongoDB — вкладені документи
- MongoDB краще підходить для швидко змінних даних

---

## 💻 Простий приклад

Уявімо колекцію `users`:

```json
{
  "name": "Oleh",
  "email": "oleh@mail.com",
  "isActive": true
}
```

Це приклад того, як дані зберігаються у MongoDB.

---

## 📝 Підсумки
- NoSQL — альтернатива реляційним базам
- MongoDB — документна база даних
- Дані зберігаються у форматі BSON
- MongoDB підходить для гнучких та масштабованих систем

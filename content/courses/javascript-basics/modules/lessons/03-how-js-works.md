# Як працює JavaScript

JavaScript — це однопоточна мова програмування, яка виконує код послідовно.
Розуміння того, як він працює «під капотом», допомагає писати більш надійний і зрозумілий код.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, як виконується JavaScript-код
- пояснити, що таке Call Stack
- зрозуміти, чому JavaScript називають однопоточною мовою

---

## 🧠 Теорія

### Однопоточність JavaScript
JavaScript виконує **одну операцію за раз**.
Він читає та виконує код **рядок за рядком**, зверху донизу.

Це означає:
- немає паралельного виконання коду
- кожна команда повинна завершитися, перш ніж почнеться наступна

---

### Виконання коду
Приклад звичайного синхронного коду:

```js
console.log("Початок");
console.log("Обробка");
console.log("Кінець");
```

Результат у консолі:
```
Початок
Обробка
Кінець
```

Код виконується строго по порядку.

---

### Call Stack
**Call Stack** — це механізм, який відстежує виконання функцій.

Простіше кажучи:
> Call Stack — це стек викликів функцій

Він працює за принципом **LIFO** (Last In — First Out / Останній увійшов — першим вийшов).

---

### Приклад роботи Call Stack

```js
function greet() {
  sayHello();
}

function sayHello() {
  console.log("Привіт!");
}

greet();
```

Порядок виконання:
1. `greet()` додається до стека
2. `sayHello()` додається до стека
3. виконується `console.log`
4. `sayHello()` видаляється зі стека
5. `greet()` видаляється зі стека

---

### Помилка переповнення стека

```js
function loop() {
  loop();
}

loop();
```

Результат:
```
Maximum call stack size exceeded
```

Причина — нескінченний виклик функцій.

---

## 📝 Підсумки
- JavaScript — однопоточна мова
- Код виконується послідовно
- Call Stack керує виконанням функцій
- Переповнення стека — поширена помилка новачків
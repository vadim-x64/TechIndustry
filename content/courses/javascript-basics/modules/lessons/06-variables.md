# Змінні в JavaScript: змінні

Змінні — це основа будь-якого JavaScript-коду.
Вони дозволяють **зберігати дані**, працювати з ними та змінювати їх під час виконання програми.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке змінні
- використовувати `var`, `let` та `const`
- вибирати правильний тип оголошення змінної

---

## 🧠 Теорія

### Що таке змінна
**Змінна** — це контейнер для зберігання даних.

Простіше кажучи:
> змінна — це ім'я, за яким скривається значення

Приклад:
```js
let age = 20;
```

Тут:
- `age` — ім'я змінної
- `20` — значення

---

### Оголошення змінних
У JavaScript є три способи оголосити змінну:

- `var`
- `let`
- `const`

---

### var (застарілий спосіб)
`var` — старий спосіб оголошення змінних.

```js
var name = "Alex";
```

Мінуси `var`:
- немає блокової області видимості
- може приводити до помилок

📌 Зараз `var` **майже не використовується**.

---

### let
`let` — сучасний спосіб оголошення змінних.

```js
let city = "Kyiv";
city = "Lviv";
```

Особливості:
- можна змінювати значення
- є блокова область видимості

---

### const
`const` використовується для значень, які **не повинні змінюватися**.

```js
const PI = 3.14;
```

📌 Спроба змінити `const` приведе до помилки.

Важливо:
- `const` захищає **посилання**, а не дані всередині об'єкта

---

### Приклад з об'єктом
```js
const user = {
  name: "John"
};

user.name = "Mike"; // ✅ працює
```

---

## 💻 Приклад використання

```js
let score = 0;
score = score + 10;

const gameName = "JS Game";

console.log(score);
console.log(gameName);
```

---

## 📝 Підсумки
- Змінні зберігають дані
- `let` — для змінюваних значень
- `const` — для незмінюваних посилань
- `var` краще уникати

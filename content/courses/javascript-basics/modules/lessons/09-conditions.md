# Умови в JavaScript: умови

Умови дозволяють програмі **приймати рішення**.
З їхньою допомогою JavaScript виконує різний код залежно від ситуації.

Без умов неможливо створити логіку, яка реагує на дії користувача.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, як працюють умови
- використовувати `if`, `else if`, `else`
- застосовувати `switch` для кількох варіантів

---

## 🧠 Теорія

### Що таке умова
**Умова** — це перевірка, результатом якої є `true` або `false`.

Приклад:
```js
age >= 18
```

Якщо умова істинна — виконується один код, якщо хибна — інший.

---

### if
Оператор `if` виконує код, **якщо умова істинна**.

```js
if (age >= 18) {
  console.log("Access granted");
}
```

Якщо умова `false`, код всередині блока не виконається.

---

### else
`else` виконується, якщо умова в `if` хибна.

```js
if (age >= 18) {
  console.log("Access granted");
} else {
  console.log("Access denied");
}
```

---

### else if
Використовується, коли є **кілька умов**.

```js
if (score >= 90) {
  grade = "A";
} else if (score >= 75) {
  grade = "B";
} else {
  grade = "C";
}
```

📌 Перевірка йде зверху донизу — виконається перший підходящий блок.

---

### switch
`switch` — альтернатива кільком `else if`.

```js
switch (day) {
  case "Monday":
    console.log("Start of week");
    break;
  case "Friday":
    console.log("Almost weekend");
    break;
  default:
    console.log("Regular day");
}
```

Важливо:
- `break` зупиняє виконання
- `default` виконується, якщо жоден case не підійшов

---

### Truthy і Falsy
В умовах JavaScript автоматично наводить значення до `true` або `false`.

Falsy значення:
- `false`
- `0`
- `""`
- `null`
- `undefined`
- `NaN`

Усі інші значення — `truthy`.

---

## 💻 Приклад використання

```js
let isLoggedIn = true;

if (isLoggedIn) {
  console.log("Welcome!");
} else {
  console.log("Please log in");
}
```

---

## 📝 Підсумки
- Умови управляють логікою програми
- `if / else` — основний інструмент
- `switch` зручний для кількох варіантів
- Truthy / Falsy важливо розуміти для умов

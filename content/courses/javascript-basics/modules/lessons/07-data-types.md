# Типи даних в JavaScript: типи даних

Типи даних визначають, **яку саме інформацію** зберігає змінна
та які операції можна з нею виконувати.

JavaScript — мова з **динамічною типізацією**, тому розуміння типів даних особливо важливе.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- знати основні типи даних у JavaScript
- розрізняти примітивні та референцні типи
- розуміти, як JavaScript працює з типами

---

## 🧠 Теорія

### Динамічна типізація
У JavaScript тип даних визначається **під час виконання**.

```js
let value = 10;
value = "text";
```

📌 Тип змінної може змінюватися — це нормально для JavaScript.

---

### Примітивні типи даних
Примітиви — це прості значення.

Основні примітивні типи:

- `number`
- `string`
- `boolean`
- `null`
- `undefined`
- `symbol`
- `bigint`

---

### Number
Тип `number` використовується для чисел.

```js
let age = 25;
let price = 99.9;
```

📌 У JavaScript **немає окремого типу для цілих і дробових чисел**.

---

### String
`string` — це рядок тексту.

```js
let name = "Alex";
let message = `Hello, ${name}`;
```

Рядки можна писати:
- в лапках `" "`
- в одинарних `' '`
- в шаблонних `` ` ` ``

---

### Boolean
Тип `boolean` має два значення:

```js
true
false
```

Використовується в умовах і логіці програми.

---

### null і undefined
- `undefined` — значення не присвоєно
- `null` — значення відсутнє навмисне

```js
let a;
let b = null;
```

---

### Symbol і BigInt
- `symbol` — унікальне значення
- `bigint` — для дуже великих чисел

```js
const id = Symbol("id");
const big = 12345678901234567890n;
```

---

### Референцні типи даних
Референцні типи зберігаються за посиланням.

Основні:
- `object`
- `array`
- `function`

---

### Object
```js
const user = {
  name: "John",
  age: 30
};
```

Об'єкти дозволяють зберігати пов'язані дані.

---

### Array
```js
const numbers = [1, 2, 3];
```

Масиви — це впорядковані колекції даних.

---

## 💻 Приклад порівняння

```js
let x = 10;
let y = x;

y = 20;

console.log(x); // 10
console.log(y); // 20
```

```js
const obj1 = { value: 10 };
const obj2 = obj1;

obj2.value = 20;

console.log(obj1.value); // 20
```

📌 Примітиви копіюються за значенням, об'єкти — за посиланням.

---

## 📝 Підсумки
- JavaScript має динамічну типізацію
- Примітиви та об'єкти працюють по-різному
- Тип даних впливає на поведінку коду
- Розуміння типів — ключ до чистого JS-коду


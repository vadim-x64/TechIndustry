# DevTools: інструменти розробника в браузері

DevTools (Developer Tools) — це вбудовані інструменти в браузері,
які допомагають розробнику **вивчати, відлагоджувати та поліпшувати веб‑додатки**.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, навіщо потрібні DevTools
- орієнтуватися в основних вкладках
- використовувати DevTools для відлагодження JavaScript

---

## 🧠 Теорія

### Що таке DevTools
DevTools — це набір інструментів, вбудованих у браузер (Chrome, Edge, Firefox).

З їхньою допомогою можна:
- дивитися HTML та CSS сторінки
- відстежувати помилки JavaScript
- аналізувати продуктивність
- працювати з мережею та запитами

Відкриваються вони комбінацією клавіш:
```
F12
```
або
```
Ctrl + Shift + I
```

---

### Вкладка Elements
Вкладка **Elements** показує структуру сторінки (HTML) та стилі (CSS).

Тут можна:
- переглядати HTML‑елементи
- тимчасово змінювати CSS
- перевіряти, які стилі застосовуються до елемента

📌 Зміни в цій вкладці **не зберігаються** після перезавантаження сторінки.

---

### Вкладка Console
**Console** — головний інструмент для роботи з JavaScript.

Вона дозволяє:
- виводити повідомлення (`console.log`)
- бачити помилки
- виконувати JavaScript‑код прямо в браузері

Приклад:
```js
console.log("Привіт з DevTools");
```

Якщо в коді є помилка, вона відразу з'явиться в Console.

---

### Вкладка Sources
Вкладка **Sources** використовується для відлагодження коду.

Тут можна:
- переглядати JavaScript‑файли
- встановлювати breakpoints (точки зупинки)
- пошагово виконувати код

📌 Це особливо корисно для пошуку складних помилок.

---

### Вкладка Network
**Network** показує всі мережеві запити сторінки.

З її допомогою можна:
- бачити запити до сервера
- перевіряти відповіді (JSON, коди статусу)
- аналізувати швидкість завантаження

👉 Часто використовується при роботі з API.

---

## 💻 Приклад використання

1. Відкрий сторінку в браузері
2. Запусти DevTools
3. Перейди на вкладку Console
4. Виконай команду:

```js
alert("DevTools працює!");
```

---

## 📝 Підсумки
- DevTools — основний інструмент веб‑розробника
- Console допомагає відлагоджувати JavaScript
- Elements — працювати з HTML та CSS
- Network — аналізувати запити
- Sources — шукати помилки в коді

---

👉 Уміння працювати з DevTools економить години розробки
та робить відлагодження в рази швидшим 🚀

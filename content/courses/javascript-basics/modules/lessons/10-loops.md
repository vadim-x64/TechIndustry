# Цикли в JavaScript: цикли

Цикли дозволяють **виконувати один і той же код кілька разів**.
Вони використовуються, коли потрібно обробити колекції даних або повторювані дії.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, навіщо потрібні цикли
- використовувати `for`, `while` та `do...while`
- керувати виконанням цикла

---

## 🧠 Теорія

### Що таке цикл
**Цикл** — це конструкція, яка повторює виконання коду,
поки виконується задана умова.

Приклад:
```js
for (let i = 0; i < 3; i++) {
  console.log(i);
}
```

---

### Цикл for
`for` використовується, коли **відомо кількість повторень**.

```js
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

Структура:
- ініціалізація
- умова
- крок

---

### Цикл while
`while` виконується, поки умова істинна.

```js
let count = 0;

while (count < 3) {
  console.log(count);
  count++;
}
```

📌 Якщо умова одразу `false`, цикл не виконається ні разу.

---

### Цикл do...while
`do...while` подібний до `while`, але виконується **мінімум один раз**.

```js
let number = 0;

do {
  console.log(number);
  number++;
} while (number < 3);
```

---

### Керування циклом
JavaScript дозволяє керувати виконанням цикла.

- `break` — повністю зупиняє цикл
- `continue` — пропускає поточну ітерацію

Приклад:
```js
for (let i = 0; i < 5; i++) {
  if (i === 2) continue;
  if (i === 4) break;
  console.log(i);
}
```

---

### Цикли та масиви
Цикли часто використовуються для перебору масивів.

```js
const numbers = [10, 20, 30];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
```

---

## 💻 Приклад використання

```js
let sum = 0;

for (let i = 1; i <= 5; i++) {
  sum += i;
}

console.log(sum);
```

---

## 📝 Підсумки
- Цикли повторюють виконання коду
- `for` — коли відомо кількість повторень
- `while` — коли важлива умова
- `do...while` виконується мінімум один раз
- `break` та `continue` керують циклом


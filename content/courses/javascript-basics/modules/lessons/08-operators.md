# Оператори в JavaScript: оператори

Оператори в JavaScript дозволяють **виконувати операції над значеннями**:
складати, порівнювати, присвоювати та керувати логікою програми.

Без операторів неможливо написати жодної реальної програми.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке оператори
- використовувати основні типи операторів
- читати та писати вирази в JavaScript

---

## 🧠 Теорія

### Що таке оператор
**Оператор** — це спеціальний символ або слово,
яке виконує операцію над одним або кількома значеннями.

Приклад:
```js
let sum = 5 + 3;
```

Тут `+` — оператор додавання.

---

### Арифметичні оператори
Використовуються для роботи з числами.

Основні арифметичні оператори:

- `+` додавання
- `-` віднімання
- `*` множення
- `/` ділення
- `%` остача від ділення
- `**` піднесення до степеня

Приклад:
```js
let result = 10 % 3; // 1
```

---

### Оператори присвоєння
Використовуються для присвоєння значень змінним.

```js
let x = 10;
x += 5; // 15
x *= 2; // 30
```

📌 Такі оператори роблять код коротшим і зрозумілішим.

---

### Оператори порівняння
Оператори порівняння повертають `true` або `false`.

Основні:
- `==` нестроге рівність
- `===` строге рівність
- `!=` нерівність
- `!==` строга нерівність
- `>` `<` `>=` `<=`

Приклад:
```js
5 === "5"; // false
5 == "5";  // true
```

📌 Рекомендується використовувати **строге порівняння (`===`)**.

---

### Логічні оператори
Використовуються для роботи з умовами.

- `&&` І
- `||` АБО
- `!` НЕ

Приклад:
```js
let isAdult = age >= 18 && hasPassport;
```

---

### Унарні оператори
Працюють з одним значенням.

```js
let count = 1;
count++;
count--;
```

---

### Тернарний оператор
Коротка форма `if...else`.

```js
let access = age >= 18 ? "allowed" : "denied";
```

📌 Робить код компактним, але не варто зловживати.

---

## 💻 Приклад використання

```js
let a = 10;
let b = 5;

let sum = a + b;
let isEqual = a === b;

console.log(sum);
console.log(isEqual);
```

---

## 📝 Підсумки
- Оператори виконують дії над даними
- Арифметичні — для чисел
- Порівняння — для умов
- Логічні — для складної логіки
- `===` безпечніший за `==`


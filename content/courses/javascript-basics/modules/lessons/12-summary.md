# Підсумок JavaScript: короткий огляд

Цей урок — **узагальнення всіх основних тем**, які ти вже вивчив.
Він допомагає структурувати знання та побачити загальну картину JavaScript.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- згадати ключові концепції JavaScript
- пов'язати теми між собою
- краще розуміти, як писати простий JS-код

---

## 🧠 Основні поняття

### Як працює JavaScript
- JavaScript виконується **послідовно**
- Він є **однопоточним**
- Виконання коду контролюється **Call Stack**

---

### Запуск JavaScript
JavaScript можна запускати:
- в браузері
- в DevTools (Console)
- за допомогою Node.js

Одна й та сама логіка може працювати в різних середовищах.

---

### Змінні та типи даних
Для зберігання даних використовуються змінні:

- `let` — змінювані значення
- `const` — незмінювані посилання

Типи даних діляться на:
- примітивні
- референцні (об'єкти, масиви, функції)

---

### Оператори та умови
Оператори дозволяють:
- виконувати обчислення
- порівнювати значення
- будувати логіку

Умови (`if`, `else`, `switch`) допомагають програмі приймати рішення.

---

### Цикли
Цикли використовуються для повторення дій:

- `for`
- `while`
- `do...while`

З їхньою допомогою зручно працювати з масивами та даними.

---

### Функції
Функції дозволяють:
- переиспользовувати код
- розбивати програму на частини
- передавати та повертати дані

Існують:
- звичайні функції
- function expressions
- arrow functions

---

## 💻 Приклад об'єднання знань

```js
function calculateSum(numbers) {
  let sum = 0;

  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }

  return sum;
}

console.log(calculateSum([1, 2, 3, 4]));
```

Цей приклад використовує:
- функцію
- цикл
- змінні
- оператори

---

## 📝 Підсумки
- JavaScript складається з простих, але важливих концепцій
- Усі теми пов'язані між собою
- Розуміння основ — ключ до вивчення складних тем
- Практика важливіша за заучування

🎉 **Вітаємо! Ти завершив теоретичний курс JavaScript**

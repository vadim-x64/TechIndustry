# Функції в JavaScript: функції

Функції дозволяють **групувати код** та використовувати його багаторазово.
Це один з найважливіших інструментів у JavaScript.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке функція
- оголошувати та викликати функції
- використовувати параметри та повертати значення

---

## 🧠 Теорія

### Що таке функція
**Функція** — це блок коду, який виконує визначену задачу
та може бути викликаний у будь-якому місці програми.

Приклад:
```js
function sayHello() {
  console.log("Привіт!");
}
```

---

### Виклик функції
Щоб код всередині функції виконався, її потрібно викликати:

```js
sayHello();
```

📌 Функція може викликатися **як завгодно багато разів**.

---

### Параметри функції
Функції можуть приймати дані — **параметри**.

```js
function greet(name) {
  console.log("Привіт, " + name);
}

greet("Alex");
```

Тут `name` — параметр функції.

---

### Повернення значення
Функція може повертати результат за допомогою `return`.

```js
function sum(a, b) {
  return a + b;
}

let result = sum(3, 5);
```

📌 Після `return` виконання функції припиняється.

---

### Function Expression
Функцію можна записати в змінну.

```js
const multiply = function(a, b) {
  return a * b;
};
```

Такі функції часто використовуються у сучасному JavaScript.

---

### Arrow Functions
Стрілкові функції — коротший синтаксис функцій.

```js
const add = (a, b) => a + b;
```

📌 Arrow functions роблять код компактнішим.

---

## 💻 Приклад використання

```js
function isAdult(age) {
  return age >= 18;
}

console.log(isAdult(20)); // true
console.log(isAdult(15)); // false
```

---

## 📝 Підсумки
- Функції дозволяють переиспользовувати код
- Параметри передають дані
- `return` повертає результат
- Arrow functions — сучасний синтаксис

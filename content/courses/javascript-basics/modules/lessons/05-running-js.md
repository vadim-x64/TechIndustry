# Запуск JavaScript: як запускати JavaScript

JavaScript можна запускати **різними способами** — не тільки в браузері.
Розуміння того, **де й як виконується JS-код**, важливе для кожного розробника.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, де може виконуватися JavaScript
- запускати JavaScript в браузері
- запускати JavaScript за допомогою Node.js

---

## 🧠 Теорія

### Де виконується JavaScript
JavaScript може виконуватися в двох основних середовищах:

- 🌐 **У браузері**
- 🖥 **На сервері (Node.js)**

Хоча мова одна й та сама, середовище виконання відрізняється.

---

### Запуск JavaScript в браузері
Найпоширеніший спосіб — запуск JS у браузері.

JavaScript можна підключити до HTML-файлу:

```html
<script src="script.js"></script>
```

Або написати прямо в HTML:

```html
<script>
  console.log("JavaScript працює в браузері");
</script>
```

📌 Браузер автоматично виконує цей код при завантаженні сторінки.

---

### Запуск через Console
JavaScript можна запускати **прямо через DevTools**.

1. Відкрий DevTools
2. Перейди на вкладку Console
3. Введи код:

```js
2 + 2
```

👉 Результат з'явиться відразу.

---

### Запуск JavaScript за допомогою Node.js
Node.js дозволяє запускати JavaScript **поза браузером**.

Приклад файлу `app.js`:

```js
console.log("Привіт з Node.js");
```

Запуск у терміналі:

```bash
node app.js
```

📌 Цей спосіб часто використовується для:
- серверних додатків
- скриптів
- автоматизації

---

### Відмінності браузера та Node.js
| Браузер | Node.js |
|-------|--------|
| Є DOM | Немає DOM |
| Робота з HTML | Робота з файлами |
| Використовується для UI | Використовується для серверів |

---

## 💻 Простий приклад

```js
let name = "JavaScript";
console.log(`Running ${name}`);
```

Цей код буде працювати **і в браузері, і в Node.js**.

---

## 📝 Підсумки
- JavaScript можна запускати різними способами
- Браузер — основний спосіб для веба
- Node.js — для серверів і скриптів
- Середовище виконання впливає на можливості JS

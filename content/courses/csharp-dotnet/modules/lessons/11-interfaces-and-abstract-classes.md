# Interfaces and Abstract Classes

Інтерфейси та абстрактні класи дозволяють будувати
**гнучку, розширювану та підтримувану архітектуру**.
Вони визначають контракти поведінки та допомагають відокремити
реалізацію від використання.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти різницю між інтерфейсами та абстрактними класами
- створювати та використовувати інтерфейси
- працювати з абстрактними класами
- обирати правильний підхід для різних задач

---

## 🧠 Теорія

### Що таке інтерфейс
**Інтерфейс** — це контракт, який описує,
**які методи повинен реалізувати клас**, але не як саме.

```csharp
interface ILogger
{
    void Log(string message);
}
```

📌 Інтерфейс не містить реалізації (за базовим підходом).

---

### Реалізація інтерфейсу
Клас реалізує інтерфейс за допомогою `:`.

```csharp
class ConsoleLogger : ILogger
{
    public void Log(string message)
    {
        Console.WriteLine(message);
    }
}
```

Клас зобовʼязаний реалізувати всі методи інтерфейсу.

---

### Навіщо потрібні інтерфейси
Інтерфейси дозволяють:
- зменшити звʼязність коду
- легко змінювати реалізацію
- спростити тестування
- будувати модульні системи

📌 Це основа Dependency Injection.

---

### Поліморфізм через інтерфейси
```csharp
ILogger logger = new ConsoleLogger();
logger.Log("Hello");
```

Код не знає, **яка саме реалізація** використовується.

---

### Кілька інтерфейсів
Клас може реалізовувати **декілька інтерфейсів**.

```csharp
interface IReadable
{
    void Read();
}

interface IWritable
{
    void Write();
}

class FileHandler : IReadable, IWritable
{
    public void Read() { }
    public void Write() { }
}
```

📌 Це альтернатива множинному наслідуванню класів.

---

### Що таке абстрактний клас
**Абстрактний клас** — це клас, який:
- не можна створити напряму
- може містити реалізацію
- може містити абстрактні методи

```csharp
abstract class Shape
{
    public abstract double GetArea();
}
```

---

### Наслідування абстрактного класу
```csharp
class Circle : Shape
{
    public double Radius { get; set; }

    public override double GetArea()
    {
        return Math.PI * Radius * Radius;
    }
}
```

📌 Абстрактні методи обовʼязково реалізуються.

---

### Абстрактні vs звичайні методи
Абстрактний клас може містити обидва типи:

```csharp
abstract class Vehicle
{
    public abstract void Move();

    public void Stop()
    {
        Console.WriteLine("Stopped");
    }
}
```

---

### Коли використовувати інтерфейс
- коли потрібен контракт
- коли клас повинен мати кілька «ролей»
- коли важлива гнучкість

### Коли використовувати абстрактний клас
- коли є спільна реалізація
- коли будується ієрархія
- коли потрібен базовий стан

---

## 💻 Повний приклад

```csharp
using System;

interface IPayment
{
    void Pay(double amount);
}

abstract class PaymentBase
{
    public void PrintReceipt()
    {
        Console.WriteLine("Receipt printed");
    }
}

class CardPayment : PaymentBase, IPayment
{
    public void Pay(double amount)
    {
        Console.WriteLine($"Paid {amount} by card");
    }
}

class Program
{
    static void Main()
    {
        IPayment payment = new CardPayment();
        payment.Pay(100);
    }
}
```

---

## 📝 Підсумки
- Інтерфейс — контракт без реалізації
- Абстрактний клас — базовий клас з логікою
- Клас може реалізовувати багато інтерфейсів
- Вибір залежить від задачі та архітектури

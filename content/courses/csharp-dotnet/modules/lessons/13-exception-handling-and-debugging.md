# Exception Handling and Debugging

Під час виконання програм можуть виникати помилки.
C# надає механізми для **обробки винятків** та інструменти
для **відлагодження коду**, що допомагає знаходити й виправляти проблеми.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке винятки
- використовувати `try`, `catch`, `finally`
- створювати власні винятки
- застосовувати базові техніки дебагінгу

---

## 🧠 Теорія

### Що таке виняток
**Виняток (exception)** — це помилка, яка виникає під час виконання програми.

Типові приклади:
- ділення на нуль
- доступ до неіснуючого індексу
- помилки введення даних
- проблеми з файлами

---

### Блок try-catch
Для обробки винятків використовується конструкція `try-catch`.

```csharp
try
{
    int x = int.Parse("abc");
}
catch
{
    Console.WriteLine("Помилка перетворення");
}
```

📌 Код у `catch` виконується, якщо сталася помилка.

---

### Обробка конкретних винятків
Краще перехоплювати **конкретні типи** винятків.

```csharp
try
{
    int result = 10 / 0;
}
catch (DivideByZeroException)
{
    Console.WriteLine("Ділення на нуль");
}
```

---

### Блок finally
`finally` виконується **завжди**, незалежно від помилки.

```csharp
try
{
    // код
}
catch
{
    // обробка
}
finally
{
    Console.WriteLine("Завершення операції");
}
```

📌 Часто використовується для звільнення ресурсів.

---

### Кілька catch-блоків
```csharp
try
{
    // код
}
catch (FormatException)
{
    Console.WriteLine("Невірний формат");
}
catch (Exception)
{
    Console.WriteLine("Загальна помилка");
}
```

---

### Кидання винятків
Можна створювати винятки вручну.

```csharp
if (age < 0)
{
    throw new ArgumentException("Вік не може бути відʼємним");
}
```

---

### Власні винятки
```csharp
class CustomException : Exception
{
    public CustomException(string message) : base(message) { }
}
```

---

## 🐞 Debugging

### Що таке дебагінг
**Debugging** — це процес пошуку та виправлення помилок у коді.

---

### Основні інструменти
У IDE (Visual Studio):
- breakpoints
- покрокове виконання
- перегляд змінних
- call stack

---

### Breakpoint
Breakpoint зупиняє виконання програми у визначеному місці,
дозволяючи перевірити стан змінних.

---

### Покрокове виконання
Дозволяє виконувати код рядок за рядком
та спостерігати за змінами стану програми.

---

## 💻 Повний приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        try
        {
            Console.Write("Enter number: ");
            int number = int.Parse(Console.ReadLine());
            Console.WriteLine(100 / number);
        }
        catch (DivideByZeroException)
        {
            Console.WriteLine("Cannot divide by zero");
        }
        catch (FormatException)
        {
            Console.WriteLine("Invalid input");
        }
        finally
        {
            Console.WriteLine("Program finished");
        }
    }
}
```

---

## 📝 Підсумки
- Винятки обробляють помилки виконання
- `try-catch-finally` забезпечує стабільність
- Краще ловити конкретні винятки
- Debugging — ключова навичка розробника

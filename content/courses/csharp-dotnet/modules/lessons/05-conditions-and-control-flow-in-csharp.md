# Conditions and Control Flow in C#

Умовні оператори та керування потоком виконання дозволяють програмі
**приймати рішення та виконувати різні гілки коду** залежно від умов.
Це основа логіки будь-якого застосунку на C#.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- використовувати `if`, `else`, `else if`
- працювати з оператором `switch`
- застосовувати логічні та порівняльні оператори
- керувати потоком виконання програми

---

## 🧠 Теорія

### Що таке умова
**Умова** — це вираз, який повертає `true` або `false`.

```csharp
int age = 20;
age >= 18
```

На основі результату C# визначає, який код виконувати далі.

---

### Оператор if
`if` виконує код, якщо умова істинна.

```csharp
if (age >= 18)
{
    Console.WriteLine("Access granted");
}
```

📌 Дужки `{}` визначають блок коду.

---

### else та else if
`else` виконується, якщо умова в `if` хибна.
`else if` дозволяє перевіряти кілька умов.

```csharp
int score = 85;

if (score >= 90)
{
    Console.WriteLine("Grade A");
}
else if (score >= 75)
{
    Console.WriteLine("Grade B");
}
else
{
    Console.WriteLine("Grade C");
}
```

---

### Порівняльні оператори
Найчастіше використовуються:

- `==` дорівнює
- `!=` не дорівнює
- `>` `<`
- `>=` `<=`

```csharp
int x = 10;
Console.WriteLine(x == 10);
```

---

### Логічні оператори
Логічні оператори дозволяють поєднувати умови:

- `&&` (AND)
- `||` (OR)
- `!` (NOT)

```csharp
if (age >= 18 && hasId)
{
    Console.WriteLine("Allowed");
}
```

---

### Оператор switch
`switch` використовується для вибору з багатьох варіантів.

```csharp
int day = 3;

switch (day)
{
    case 1:
        Console.WriteLine("Monday");
        break;
    case 2:
        Console.WriteLine("Tuesday");
        break;
    case 3:
        Console.WriteLine("Wednesday");
        break;
    default:
        Console.WriteLine("Unknown day");
        break;
}
```

📌 `break` обовʼязковий для виходу з case.

---

### Тернарний оператор
Скорочений варіант `if-else`.

```csharp
string result = age >= 18 ? "Adult" : "Minor";
```

---

## 💻 Простий приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        int temperature = 30;

        if (temperature > 25)
        {
            Console.WriteLine("Hot");
        }
        else
        {
            Console.WriteLine("Normal");
        }
    }
}
```

---

## 📝 Підсумки
- Умови керують логікою програми
- `if / else / else if` — базові інструменти
- `switch` зручний для багатьох варіантів
- Логічні оператори поєднують перевірки

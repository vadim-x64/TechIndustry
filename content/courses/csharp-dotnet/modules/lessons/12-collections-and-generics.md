# Collections and Generics

Колекції та generics — це основа роботи з наборами даних у C#.
Вони дозволяють **зберігати, обробляти та типобезпечно використовувати**
групи обʼєктів у програмах будь-якої складності.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- працювати з основними колекціями C#
- розуміти різницю між масивами та колекціями
- використовувати generics
- писати типобезпечний та гнучкий код

---

## 🧠 Теорія

### Що таке колекція
**Колекція** — це структура даних,
яка дозволяє зберігати кілька значень в одному обʼєкті.

Колекції використовуються, коли:
- кількість елементів змінюється
- потрібно додавати або видаляти елементи
- необхідна зручна робота з даними

---

### Масиви
Масив — це базова структура даних з фіксованим розміром.

```csharp
int[] numbers = { 1, 2, 3 };
```

📌 Розмір масиву не можна змінити після створення.

---

### List<T>
`List<T>` — найпопулярніша колекція в C#.

```csharp
List<int> list = new List<int>();
list.Add(10);
list.Add(20);
```

Переваги:
- динамічний розмір
- зручні методи (`Add`, `Remove`, `Count`)

---

### Перебір List
```csharp
foreach (int item in list)
{
    Console.WriteLine(item);
}
```

---

### Dictionary<TKey, TValue>
`Dictionary` зберігає пари ключ–значення.

```csharp
Dictionary<int, string> users = new Dictionary<int, string>();
users.Add(1, "Anna");
users.Add(2, "Oleh");
```

📌 Ключі повинні бути унікальними.

---

### Доступ до елементів Dictionary
```csharp
string name = users[1];
```

Або безпечніше:

```csharp
if (users.TryGetValue(1, out string userName))
{
    Console.WriteLine(userName);
}
```

---

### Інші популярні колекції
- `Queue<T>` — черга
- `Stack<T>` — стек
- `HashSet<T>` — унікальні значення

```csharp
HashSet<int> ids = new HashSet<int>();
ids.Add(1);
ids.Add(1); // не додасться
```

---

## 🧬 Generics

### Що таке generics
**Generics** дозволяють створювати класи та методи,
які працюють з різними типами даних,
зберігаючи типобезпеку.

```csharp
List<string> names = new List<string>();
```

---

### Навіщо потрібні generics
Generics:
- запобігають помилкам типів
- не потребують casting
- покращують читабельність коду
- підвищують продуктивність

---

### Generic-метод
```csharp
void Print<T>(T value)
{
    Console.WriteLine(value);
}
```

Використання:

```csharp
Print<int>(5);
Print<string>("Hello");
```

---

### Generic-клас
```csharp
class Box<T>
{
    public T Value { get; set; }
}
```

```csharp
Box<int> intBox = new Box<int>();
intBox.Value = 10;
```

---

## 💻 Повний приклад

```csharp
using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        List<string> fruits = new List<string> { "Apple", "Banana" };

        Dictionary<int, string> users = new Dictionary<int, string>
        {
            { 1, "Anna" },
            { 2, "Oleh" }
        };

        foreach (var fruit in fruits)
        {
            Console.WriteLine(fruit);
        }
    }
}
```

---

## 📝 Підсумки
- Колекції зберігають набори даних
- `List` і `Dictionary` — найуживаніші
- Generics забезпечують типобезпеку
- Generic-код гнучкий і перевикористовуваний

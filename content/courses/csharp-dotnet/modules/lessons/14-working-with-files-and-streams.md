# Working with Files and Streams

Робота з файлами — важлива частина багатьох застосунків.
C# надає зручні інструменти для **читання, запису та обробки файлів**
за допомогою потоків (streams).

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- читати та записувати файли
- розуміти, що таке потоки
- використовувати `File` та `FileStream`
- безпечно працювати з ресурсами

---

## 🧠 Теорія

### Що таке файл
**Файл** — це спосіб зберігання даних на диску.
Програми часто використовують файли для:
- збереження налаштувань
- логування
- імпорту та експорту даних

---

### Потоки (Streams)
**Потік** — це абстракція для послідовної роботи з даними.

Основні типи потоків у C#:
- `FileStream`
- `StreamReader`
- `StreamWriter`
- `MemoryStream`

📌 Потоки дозволяють працювати з великими обʼємами даних.

---

### Простий запис у файл
Найпростіший спосіб запису — клас `File`.

```csharp
File.WriteAllText("data.txt", "Hello file");
```

---

### Читання з файлу
```csharp
string content = File.ReadAllText("data.txt");
Console.WriteLine(content);
```

📌 Ці методи підходять для невеликих файлів.

---

### Запис з використанням StreamWriter
```csharp
using (StreamWriter writer = new StreamWriter("log.txt"))
{
    writer.WriteLine("Application started");
}
```

---

### Читання з використанням StreamReader
```csharp
using (StreamReader reader = new StreamReader("log.txt"))
{
    string line;
    while ((line = reader.ReadLine()) != null)
    {
        Console.WriteLine(line);
    }
}
```

---

### Конструкція using
`using` автоматично закриває потік
та звільняє ресурси.

📌 Це найкраща практика роботи з файлами.

---

### FileStream
`FileStream` надає низькорівневий доступ до файлів.

```csharp
using (FileStream fs = new FileStream("data.bin", FileMode.OpenOrCreate))
{
    // робота з байтами
}
```

---

### Перевірка існування файлу
```csharp
if (File.Exists("data.txt"))
{
    Console.WriteLine("File exists");
}
```

---

## 💻 Повний приклад

```csharp
using System;
using System.IO;

class Program
{
    static void Main()
    {
        string path = "example.txt";

        File.WriteAllText(path, "Hello C#");

        string text = File.ReadAllText(path);
        Console.WriteLine(text);
    }
}
```

---

## 📝 Підсумки
- Файли використовуються для збереження даних
- Потоки керують читанням і записом
- `using` гарантує звільнення ресурсів
- `File` зручний для простих операцій

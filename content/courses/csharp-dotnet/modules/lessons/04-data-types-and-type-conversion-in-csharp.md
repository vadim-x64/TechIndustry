# Data Types and Type Conversion in C#

Типи даних у C# визначають, **яку інформацію може зберігати змінна**
та які операції з нею дозволені. Правильне розуміння типів і приведення
типів є ключовим для безпечного та передбачуваного коду.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- знати основні типи даних у C#
- розрізняти value types і reference types
- виконувати явне та неявне приведення типів
- уникати типових помилок конвертації

---

## 🧠 Теорія

### Основні категорії типів
У C# типи даних поділяються на дві великі групи:

- **Value types** (значимі типи)
- **Reference types** (посилальні типи)

Це впливає на зберігання даних у памʼяті та поведінку змінних.

---

### Value types
Value types зберігають **саме значення**.

Приклади:
- `int`
- `double`
- `bool`
- `char`
- `struct`

```csharp
int a = 10;
int b = a;
b = 20;
```

📌 Зміна `b` не впливає на `a`.

---

### Reference types
Reference types зберігають **посилання на обʼєкт** у памʼяті.

Приклади:
- `string`
- `class`
- `array`
- `object`

```csharp
string s1 = "Hello";
string s2 = s1;
```

📌 Обидві змінні вказують на один обʼєкт.

---

### Числові типи
Найпоширеніші числові типи:

- `int` — 32-бітне ціле число
- `long` — 64-бітне ціле число
- `float` — число з плаваючою комою
- `double` — точніше дробове число
- `decimal` — фінансові обчислення

```csharp
double price = 99.99;
decimal salary = 1200.50m;
```

---

### Логічний та символьний типи
```csharp
bool isOnline = true;
char letter = 'A';
```

📌 `char` використовує одинарні лапки.

---

### Рядки (string)
`string` — це reference type, але має особливу поведінку (immutable).

```csharp
string name = "Anna";
name = name + " Smith";
```

📌 Старий рядок не змінюється — створюється новий.

---

## 🔄 Приведення типів

### Неявне приведення
Відбувається автоматично, коли **втрата даних неможлива**.

```csharp
int x = 10;
double y = x;
```

---

### Явне приведення (casting)
Потрібне, коли можливі втрати даних.

```csharp
double d = 9.7;
int i = (int)d;
```

📌 Дробова частина буде втрачена.

---

### Convert
Клас `Convert` дозволяє безпечніше перетворювати типи.

```csharp
string text = "123";
int number = Convert.ToInt32(text);
```

---

### TryParse
Найбезпечніший спосіб перетворення.

```csharp
string value = "abc";
bool result = int.TryParse(value, out int num);
```

`TryParse`:
- не кидає виняток
- повертає `true` або `false`

---

## 💻 Простий приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        string input = "42";
        if (int.TryParse(input, out int number))
        {
            Console.WriteLine(number + 10);
        }
    }
}
```

---

## 📝 Підсумки
- Типи визначають поведінку даних
- Value та Reference types працюють по-різному
- Casting може призвести до втрати даних
- `TryParse` — найбезпечніший варіант

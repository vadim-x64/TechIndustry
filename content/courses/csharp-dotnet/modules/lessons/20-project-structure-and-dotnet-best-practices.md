# Project Structure and .NET Best Practices

Правильна структура проєкту та дотримання best practices
допомагають створювати **масштабовані, підтримувані та надійні**
застосунки на платформі .NET.
Цей урок підсумовує курс і показує, як писати «production-ready» код.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- організовувати структуру .NET-проєкту
- дотримуватися best practices C# та ASP.NET Core
- розуміти базові архітектурні підходи
- уникати типових помилок початківців

---

## 🧠 Теорія

### Навіщо важлива структура проєкту
Добре організований проєкт:
- легше підтримувати
- простіше масштабувати
- швидше онбордити нових розробників
- зменшує кількість помилок

📌 Структура важлива так само, як і код.

---

### Типова структура Web API
Приклад структури ASP.NET Core Web API:

```
/Controllers
/Models
/DTOs
/Data
/Services
/Interfaces
Program.cs
appsettings.json
```

---

### Controllers
Контролери:
- приймають HTTP-запити
- повертають відповіді
- не повинні містити бізнес-логіку

📌 Контролер = тонкий шар.

---

### Services
Сервіси містять бізнес-логіку.

```csharp
public class UserService
{
    public string GetUserName(int id)
    {
        return "Anna";
    }
}
```

---

### Interfaces
Інтерфейси дозволяють:
- зменшити звʼязність
- легко міняти реалізацію
- спростити тестування

```csharp
public interface IUserService
{
    string GetUserName(int id);
}
```

---

### Dependency Injection
Використовуй DI для всіх сервісів.

```csharp
builder.Services.AddScoped<IUserService, UserService>();
```

📌 Це стандарт у ASP.NET Core.

---

### DTO та Models
- **Models** — відображають дані БД
- **DTO** — використовуються для API

📌 Не повертай Entity напряму з API.

---

### Конфігурація
Використовуй `appsettings.json` для налаштувань.

```json
{
  "ConnectionStrings": {
    "Default": "Server=..."
  }
}
```

---

### Логування
ASP.NET Core має вбудоване логування.

```csharp
logger.LogInformation("Application started");
```

📌 Логи — ключ до підтримки production.

---

### Обробка помилок
Використовуй middleware для глобальної обробки помилок.

- централізовано
- без дублювання
- з коректними HTTP-кодами

---

### Async everywhere
У Web API:
- використовуй `async/await`
- не блокуй потоки
- застосовуй `Task`

---

### Чистий код
Основні правила:
- зрозумілі назви
- короткі методи
- одна відповідальність
- мінімум логіки в контролерах

---

## 💻 Приклад архітектури

```text
Controller → Service → Repository → Database
```

Це класичний підхід для backend-проєктів.

---

## 📝 Підсумки
- Структура визначає якість проєкту
- Best practices економлять час у майбутньому
- DI, DTO та Services — стандарт .NET
- Чистий код = підтримуваний код

---

🎉 **Вітаємо! Ти завершив теоретичний курс C# & .NET**


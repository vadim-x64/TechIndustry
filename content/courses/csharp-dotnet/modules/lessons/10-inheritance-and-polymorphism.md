# Inheritance and Polymorphism

Наслідування та поліморфізм — це ключові принципи ООП,
які дозволяють **перевикористовувати код, розширювати функціональність**
та будувати гнучкі архітектури застосунків на C#.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке наслідування
- використовувати ключове слово `:` для наслідування
- перевизначати методи
- розуміти принцип поліморфізму
- працювати з базовими та похідними класами

---

## 🧠 Теорія

### Що таке наслідування
**Наслідування** дозволяє створювати нові класи
на основі вже існуючих.

Похідний клас:
- успадковує поля та методи
- може додавати нову поведінку
- може змінювати існуючу логіку

```csharp
class Animal
{
    public void Eat()
    {
        Console.WriteLine("Animal is eating");
    }
}

class Dog : Animal
{
}
```

📌 `Dog` автоматично отримує метод `Eat`.

---

### Навіщо використовувати наслідування
Наслідування допомагає:
- уникати дублювання коду
- будувати ієрархії класів
- спрощувати підтримку системи

---

### Віртуальні методи
Щоб дозволити перевизначення методів,
у базовому класі використовується `virtual`.

```csharp
class Animal
{
    public virtual void Speak()
    {
        Console.WriteLine("Animal makes a sound");
    }
}
```

---

### Перевизначення методів
У похідному класі використовується `override`.

```csharp
class Dog : Animal
{
    public override void Speak()
    {
        Console.WriteLine("Dog barks");
    }
}
```

📌 Це і є основа поліморфізму.

---

### Поліморфізм
**Поліморфізм** означає «багато форм».

Один і той самий метод
може поводитись по-різному
залежно від типу обʼєкта.

```csharp
Animal a = new Dog();
a.Speak();
```

Результат:
```
Dog barks
```

---

### Ключове слово base
`base` дозволяє звернутися до реалізації базового класу.

```csharp
class Dog : Animal
{
    public override void Speak()
    {
        base.Speak();
        Console.WriteLine("Dog barks loudly");
    }
}
```

---

### Заборона наслідування
Клас можна зробити таким, що не наслідується,
використовуючи `sealed`.

```csharp
sealed class FinalClass
{
}
```

---

### Обмеження наслідування
C# **не підтримує множинне наслідування класів**,
але підтримує інтерфейси (про це — у наступних уроках).

---

## 💻 Повний приклад

```csharp
using System;

class Animal
{
    public virtual void Speak()
    {
        Console.WriteLine("Animal sound");
    }
}

class Cat : Animal
{
    public override void Speak()
    {
        Console.WriteLine("Cat meows");
    }
}

class Program
{
    static void Main()
    {
        Animal animal = new Cat();
        animal.Speak();
    }
}
```

---

## 📝 Підсумки
- Наслідування дозволяє перевикористовувати код
- `virtual` і `override` керують поведінкою методів
- Поліморфізм забезпечує гнучкість
- Базовий тип може посилатись на похідний обʼєкт

# Setting Up .NET Environment

Перед початком розробки на C# необхідно **правильно налаштувати середовище .NET**.
Це дозволить створювати, запускати та відлагоджувати застосунки
швидко та без зайвих проблем.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- встановити .NET SDK
- перевірити правильність встановлення
- створити перший .NET-проєкт
- підготувати середовище для роботи з C#

---

## 🧠 Теорія

### Що таке .NET SDK
**.NET SDK** — це набір інструментів для розробки .NET-застосунків.

Він включає:
- компілятор C#
- стандартні бібліотеки
- CLI-інструменти
- шаблони проєктів

📌 Без SDK неможливо створювати або запускати .NET-проєкти.

---

### Встановлення .NET
.NET SDK можна завантажити з офіційного сайту Microsoft:

- https://dotnet.microsoft.com

Під час встановлення:
- обери останню стабільну версію
- встанови SDK (не лише Runtime)

---

### Перевірка встановлення
Після встановлення відкрий термінал і виконай:

```bash
dotnet --version
```

Якщо версія відображається — .NET встановлений коректно.

---

### Інструмент dotnet CLI
**dotnet CLI** — це командний інструмент для роботи з .NET.

Основні команди:
- `dotnet new`
- `dotnet run`
- `dotnet build`
- `dotnet test`

```bash
dotnet new --list
```

Ця команда покаже доступні шаблони проєктів.

---

### Створення першого проєкту
Для створення консольного застосунку:

```bash
dotnet new console -n HelloDotNet
cd HelloDotNet
dotnet run
```

📌 Після запуску ти побачиш повідомлення у консолі.

---

### Структура .NET-проєкту
Типовий консольний проєкт містить:

- `Program.cs` — точка входу
- `.csproj` — файл конфігурації проєкту
- `bin/` та `obj/` — службові папки

---

### IDE та редактори коду
Для розробки на C# найчастіше використовують:

- Visual Studio
- Visual Studio Code
- Rider

📌 Visual Studio — найпотужніше середовище для .NET.

---

## 💻 Простий приклад

```csharp
Console.WriteLine("Environment is ready!");
```

Цей код можна виконати одразу після створення проєкту.

---

## 📝 Підсумки
- .NET SDK необхідний для розробки
- dotnet CLI керує проєктами
- Перший проєкт створюється за кілька команд
- Правильне середовище економить час


# Classes and Objects

Класи та обʼєкти — це серце обʼєктно-орієнтованого програмування.
Вони дозволяють **створювати власні типи даних**,
які поєднують стан (дані) та поведінку (методи).

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- створювати власні класи
- працювати з обʼєктами
- використовувати поля та властивості
- розуміти роль конструкторів
- організовувати код за допомогою класів

---

## 🧠 Теорія

### Клас як шаблон
**Клас** — це опис того, яким буде обʼєкт.
Він визначає:
- які дані зберігаються
- які дії можна виконувати

```csharp
class User
{
    public string Name;
    public int Age;
}
```

📌 Клас сам по собі не виконується — він лише опис.

---

### Обʼєкт як екземпляр класу
**Обʼєкт** — це конкретний екземпляр класу,
створений у памʼяті під час виконання програми.

```csharp
User user = new User();
user.Name = "Anna";
user.Age = 25;
```

Кожен обʼєкт має власний стан.

---

### Поля класу
**Поля** зберігають дані обʼєкта.

```csharp
class Product
{
    public string Title;
    public double Price;
}
```

📌 Поля зазвичай роблять `private`, а доступ надають через властивості.

---

### Властивості (Properties)
Властивості — це безпечний спосіб доступу до даних.

```csharp
class Product
{
    private double price;

    public double Price
    {
        get { return price; }
        set { price = value; }
    }
}
```

Вони дозволяють контролювати зміну значень.

---

### Автовластивості
C# дозволяє скорочений синтаксис:

```csharp
class Product
{
    public string Title { get; set; }
    public double Price { get; set; }
}
```

📌 Це найпоширеніший варіант у реальних проєктах.

---

### Конструктори
**Конструктор** викликається під час створення обʼєкта
і використовується для ініціалізації даних.

```csharp
class User
{
    public string Name;

    public User(string name)
    {
        Name = name;
    }
}
```

Використання:

```csharp
User user = new User("Oleh");
```

---

### Конструктор за замовчуванням
Якщо конструктор не визначений,
C# створює його автоматично.

```csharp
User user = new User();
```

---

### Кілька конструкторів
Клас може мати кілька конструкторів.

```csharp
class User
{
    public string Name;
    public int Age;

    public User(string name)
    {
        Name = name;
    }

    public User(string name, int age)
    {
        Name = name;
        Age = age;
    }
}
```

📌 Це називається перевантаженням конструкторів.

---

## 💻 Повний приклад

```csharp
using System;

class Person
{
    public string Name { get; set; }
    public int Age { get; set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    public void Introduce()
    {
        Console.WriteLine($"My name is {Name}, I am {Age} years old.");
    }
}

class Program
{
    static void Main()
    {
        Person p = new Person("Maria", 30);
        p.Introduce();
    }
}
```

---

## 📝 Підсумки
- Клас — це шаблон
- Обʼєкт — екземпляр класу
- Поля зберігають стан
- Властивості контролюють доступ
- Конструктори ініціалізують обʼєкти

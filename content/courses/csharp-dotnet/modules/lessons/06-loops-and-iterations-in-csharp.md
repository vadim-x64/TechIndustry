# Loops and Iterations in C#

Цикли дозволяють **повторювати виконання коду**
декілька разів без дублювання логіки.
У C# цикли широко використовуються для роботи з колекціями,
даними та алгоритмами.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- використовувати цикли `for`, `while`, `do-while`
- працювати з циклом `foreach`
- керувати виконанням циклів
- розуміти, коли який цикл використовувати

---

## 🧠 Теорія

### Що таке цикл
**Цикл** — це конструкція, яка повторює виконання блоку коду,
поки виконується певна умова або перебирається колекція.

Цикли допомагають:
- обробляти списки даних
- виконувати обчислення
- уникати повторення коду

---

### Цикл for
`for` використовується, коли відома кількість ітерацій.

```csharp
for (int i = 0; i < 5; i++)
{
    Console.WriteLine(i);
}
```

Структура:
- ініціалізація
- умова
- крок

---

### Цикл while
`while` виконується, поки умова істинна.

```csharp
int count = 0;

while (count < 3)
{
    Console.WriteLine(count);
    count++;
}
```

📌 Якщо умова завжди `true`, цикл стане нескінченним.

---

### Цикл do-while
`do-while` виконується **хоча б один раз**, навіть якщо умова хибна.

```csharp
int number = 5;

do
{
    Console.WriteLine(number);
}
while (number < 3);
```

---

### Цикл foreach
`foreach` використовується для перебору колекцій.

```csharp
string[] names = { "Anna", "Oleh", "Maria" };

foreach (string name in names)
{
    Console.WriteLine(name);
}
```

📌 `foreach` безпечніший та читабельніший для колекцій.

---

### Керування циклом
C# надає спеціальні оператори:

- `break` — зупиняє цикл
- `continue` — переходить до наступної ітерації

```csharp
for (int i = 0; i < 5; i++)
{
    if (i == 3)
        break;

    Console.WriteLine(i);
}
```

---

### Вкладені цикли
Цикли можуть бути вкладені один в один.

```csharp
for (int i = 1; i <= 2; i++)
{
    for (int j = 1; j <= 3; j++)
    {
        Console.WriteLine($"{i}, {j}");
    }
}
```

📌 Вкладені цикли часто використовуються при роботі з таблицями.

---

## 💻 Простий приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        int sum = 0;

        for (int i = 1; i <= 5; i++)
        {
            sum += i;
        }

        Console.WriteLine(sum);
    }
}
```

---

## 📝 Підсумки
- Цикли дозволяють повторювати код
- `for` — для лічильників
- `while` і `do-while` — для умов
- `foreach` — для колекцій
- `break` і `continue` керують виконанням


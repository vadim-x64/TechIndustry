# Introduction to ASP.NET Core

ASP.NET Core — це сучасний фреймворк для створення
**швидких, масштабованих та кросплатформених вебзастосунків**
на платформі .NET. Він широко використовується для backend-розробки
та створення REST API.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке ASP.NET Core
- знати, для чого використовується цей фреймворк
- створювати перший ASP.NET Core застосунок
- орієнтуватися в базовій структурі проєкту

---

## 🧠 Теорія

### Що таке ASP.NET Core
**ASP.NET Core** — це фреймворк для розробки вебзастосунків і сервісів,
побудований на платформі .NET.

Його основні переваги:
- висока продуктивність
- кросплатформеність (Windows, Linux, macOS)
- модульна архітектура
- активна спільнота

---

### Де використовується ASP.NET Core
ASP.NET Core застосовується для:
- створення REST API
- backend для веб- і мобільних застосунків
- мікросервісної архітектури
- корпоративних систем

📌 Це стандарт де-факто для backend на .NET.

---

### Типи проєктів
У ASP.NET Core існує кілька типів проєктів:

- Web API
- MVC
- Razor Pages
- Minimal API

Для backend-розробки найчастіше використовують **Web API**.

---

### Створення першого проєкту
Створення Web API через CLI:

```bash
dotnet new webapi -n MyApi
cd MyApi
dotnet run
```

Після запуску сервер буде доступний за локальною адресою.

---

### Структура проєкту
Типовий ASP.NET Core проєкт містить:

- `Program.cs` — точка входу
- `Controllers/` — контролери API
- `appsettings.json` — конфігурація
- `Properties/` — налаштування запуску

---

### Program.cs
У `Program.cs` налаштовується застосунок.

```csharp
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapGet("/", () => "Hello ASP.NET Core");

app.Run();
```

---

### Middleware
**Middleware** — це компоненти,
які обробляють HTTP-запити по черзі.

Приклади:
- логування
- автентифікація
- обробка помилок

---

### Dependency Injection
ASP.NET Core має вбудований **Dependency Injection**.

```csharp
builder.Services.AddControllers();
```

📌 DI спрощує тестування та архітектуру.

---

## 💻 Простий приклад

```csharp
using Microsoft.AspNetCore.Mvc;

[ApiController]
[Route("[controller]")]
public class HelloController : ControllerBase
{
    [HttpGet]
    public string Get()
    {
        return "Hello from API";
    }
}
```

---

## 📝 Підсумки
- ASP.NET Core — фреймворк для backend
- Підходить для API та вебзастосунків
- Має сучасну архітектуру
- Працює кросплатформено

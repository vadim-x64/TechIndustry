# LINQ Basics and Querying Data

LINQ (Language Integrated Query) — це потужний інструмент у C#,
який дозволяє **зручно та читабельно працювати з колекціями даних**,
виконуючи фільтрацію, сортування та агрегацію.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке LINQ
- використовувати LINQ для роботи з колекціями
- писати базові LINQ-запити
- застосовувати методи `Where`, `Select`, `OrderBy`

---

## 🧠 Теорія

### Що таке LINQ
**LINQ** — це набір можливостей у C#,
який дозволяє виконувати запити до даних
безпосередньо в коді.

LINQ можна застосовувати до:
- колекцій (`List`, `Array`)
- баз даних
- XML
- інших джерел даних

---

### Чому LINQ зручний
LINQ дозволяє:
- писати менше коду
- робити код читабельнішим
- уникати складних циклів
- працювати з даними декларативно

---

### LINQ і колекції
Найчастіше LINQ використовується разом з колекціями.

```csharp
List<int> numbers = new List<int> { 1, 2, 3, 4, 5 };
```

---

### Where — фільтрація
```csharp
var result = numbers.Where(n => n > 3);
```

Цей запит вибере всі числа більші за 3.

---

### Select — вибір та перетворення
```csharp
var squares = numbers.Select(n => n * n);
```

📌 `Select` дозволяє трансформувати дані.

---

### OrderBy — сортування
```csharp
var sorted = numbers.OrderBy(n => n);
```

Або за спаданням:

```csharp
var desc = numbers.OrderByDescending(n => n);
```

---

### Комбінування методів
LINQ-запити можна ланцюжити.

```csharp
var query = numbers
    .Where(n => n > 2)
    .Select(n => n * 10)
    .OrderBy(n => n);
```

---

### First, FirstOrDefault
```csharp
int first = numbers.First();
int safeFirst = numbers.FirstOrDefault();
```

📌 `FirstOrDefault` повертає значення за замовчуванням, якщо елементів немає.

---

### Count, Any
```csharp
int count = numbers.Count();
bool hasEven = numbers.Any(n => n % 2 == 0);
```

---

### LINQ Query Syntax
Окрім методів, існує синтаксис запитів.

```csharp
var query =
    from n in numbers
    where n > 2
    select n;
```

📌 Обидва варіанти рівноцінні.

---

## 💻 Повний приклад

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

class Program
{
    static void Main()
    {
        List<string> names = new List<string> { "Anna", "Oleh", "Maria" };

        var filtered =
            names
            .Where(n => n.Length > 4)
            .OrderBy(n => n);

        foreach (var name in filtered)
        {
            Console.WriteLine(name);
        }
    }
}
```

---

## 📝 Підсумки
- LINQ спрощує роботу з даними
- `Where`, `Select`, `OrderBy` — базові методи
- Запити можна комбінувати
- Код стає чистішим і зрозумілішим

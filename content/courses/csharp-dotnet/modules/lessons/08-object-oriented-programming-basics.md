# Object-Oriented Programming Basics

Обʼєктно-орієнтоване програмування (ООП) — це підхід,
який дозволяє **моделювати реальний світ у коді**
та будувати великі, зрозумілі й масштабовані застосунки.
C# повністю підтримує принципи ООП.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке ООП
- знати основні принципи ООП
- розуміти, навіщо потрібні класи та обʼєкти
- читати та писати простий ООП-код

---

## 🧠 Теорія

### Що таке ООП
**Обʼєктно-орієнтоване програмування** — це стиль програмування,
у якому програма будується з обʼєктів,
що взаємодіють між собою.

Обʼєкт поєднує:
- дані (поля)
- поведінку (методи)

---

### Навіщо потрібне ООП
ООП дозволяє:
- структурувати великий код
- зменшувати дублювання
- спрощувати підтримку
- повторно використовувати код

📌 Саме тому ООП використовується у більшості сучасних мов.

---

### Клас і обʼєкт
**Клас** — це шаблон або опис.
**Обʼєкт** — це конкретний екземпляр класу.

```csharp
class User
{
    public string Name;
}
```

```csharp
User user = new User();
user.Name = "Anna";
```

---

### Поля та методи
Класи можуть містити:
- поля (змінні класу)
- методи (функції класу)

```csharp
class Car
{
    public string Brand;

    public void Drive()
    {
        Console.WriteLine("Car is driving");
    }
}
```

---

### Чотири принципи ООП
ООП базується на чотирьох ключових принципах:

1. Інкапсуляція  
2. Абстракція  
3. Наслідування  
4. Поліморфізм  

У цьому уроці розглянемо їх загально.

---

### Інкапсуляція
**Інкапсуляція** — це приховування внутрішніх деталей класу
та контроль доступу до даних.

```csharp
class Account
{
    private double balance;

    public void Deposit(double amount)
    {
        balance += amount;
    }
}
```

📌 `private` захищає дані від прямого доступу.

---

### Абстракція
**Абстракція** дозволяє показувати лише важливі деталі,
приховуючи складну реалізацію.

Користувач знає **що робить обʼєкт**, а не **як саме**.

---

### Наслідування
**Наслідування** дозволяє одному класу
успадковувати властивості іншого.

```csharp
class Animal
{
    public void Eat()
    {
        Console.WriteLine("Eating");
    }
}

class Dog : Animal
{
}
```

---

### Поліморфізм
**Поліморфізм** дозволяє використовувати один інтерфейс
для різної поведінки.

📌 Детально ми розглянемо це у наступних уроках.

---

## 💻 Простий приклад

```csharp
using System;

class Person
{
    public string Name;

    public void SayHello()
    {
        Console.WriteLine($"Hello, my name is {Name}");
    }
}

class Program
{
    static void Main()
    {
        Person p = new Person();
        p.Name = "Oleh";
        p.SayHello();
    }
}
```

---

## 📝 Підсумки
- ООП моделює реальний світ
- Класи — це шаблони
- Обʼєкти — це екземпляри класів
- Інкапсуляція захищає дані
- ООП спрощує масштабування проєктів

# Methods and Parameters

Методи дозволяють **структурувати код, уникати повторень**
та робити програми зрозумілими й масштабованими.
У C# методи є основним інструментом організації логіки.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- створювати власні методи
- використовувати параметри
- повертати значення з методів
- розуміти різницю між `void` та методами з `return`

---

## 🧠 Теорія

### Що таке метод
**Метод** — це блок коду, який виконує певну задачу
і може бути викликаний багато разів.

```csharp
void SayHello()
{
    Console.WriteLine("Hello!");
}
```

📌 Метод оголошується всередині класу.

---

### Виклик методу
Щоб метод виконався, його потрібно викликати.

```csharp
SayHello();
```

Метод можна використовувати в різних частинах програми.

---

### Методи з параметрами
Методи можуть приймати дані через параметри.

```csharp
void Greet(string name)
{
    Console.WriteLine($"Hello, {name}");
}
```

Виклик:

```csharp
Greet("Anna");
```

---

### Кілька параметрів
```csharp
int Add(int a, int b)
{
    return a + b;
}
```

📌 Порядок параметрів має значення.

---

### Повернення значення
Методи можуть повертати результат за допомогою `return`.

```csharp
int Square(int x)
{
    return x * x;
}
```

Після `return` виконання методу завершується.

---

### void-методи
Методи з типом `void` **не повертають значення**.

```csharp
void PrintNumber(int number)
{
    Console.WriteLine(number);
}
```

---

### Значення за замовчуванням
Параметри можуть мати значення за замовчуванням.

```csharp
void Log(string message = "Default message")
{
    Console.WriteLine(message);
}
```

---

### Іменовані параметри
C# дозволяє передавати параметри за іменем.

```csharp
Greet(name: "Oleh");
```

Це покращує читабельність коду.

---

### Перевантаження методів
Методи можуть мати однакову назву,
але різні параметри.

```csharp
int Multiply(int a, int b)
{
    return a * b;
}

double Multiply(double a, double b)
{
    return a * b;
}
```

📌 Це називається **method overloading**.

---

## 💻 Простий приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        int result = Add(5, 3);
        Console.WriteLine(result);
    }

    static int Add(int x, int y)
    {
        return x + y;
    }
}
```

---

## 📝 Підсумки
- Методи структурують програму
- Параметри передають дані
- `return` повертає результат
- `void` — для дій без результату
- Перевантаження робить API зручнішим

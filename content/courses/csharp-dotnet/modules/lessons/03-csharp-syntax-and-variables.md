# C# Syntax and Variables

C# має строгий та чіткий синтаксис,
який допомагає писати **зрозумілий, безпечний і підтримуваний код**.
У цьому уроці ми розглянемо базові правила мови та роботу зі змінними.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти базовий синтаксис C#
- створювати та використовувати змінні
- відрізняти типи змінних
- писати прості програми на C#

---

## 🧠 Теорія

### Структура C#-програми
Кожна C#-програма має точку входу — метод `Main`.

```csharp
using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Hello C#");
    }
}
```

📌 Код у C# завжди знаходиться всередині класів.

---

### Інструкції та крапка з комою
Кожна інструкція в C# завершується **крапкою з комою** `;`.

```csharp
int x = 10;
Console.WriteLine(x);
```

---

### Коментарі
Коментарі використовуються для пояснення коду
і не впливають на виконання програми.

```csharp
// Однорядковий коментар

/*
  Багаторядковий
  коментар
*/
```

---

### Змінні
**Змінна** — це іменоване місце в памʼяті для зберігання даних.

```csharp
int age = 25;
string name = "Anna";
```

📌 У C# тип змінної вказується **явно**.

---

### Типи змінних
Основні типи:

- `int` — цілі числа
- `double` — дробові числа
- `string` — рядки
- `bool` — логічні значення
- `char` — один символ

```csharp
bool isActive = true;
double price = 99.99;
```

---

### Оголошення без ініціалізації
```csharp
int count;
count = 5;
```

📌 Змінну потрібно ініціалізувати перед використанням.

---

### var — неявна типізація
C# дозволяє використовувати `var`,
але тип визначається **під час компіляції**.

```csharp
var number = 10;      // int
var text = "Hello";  // string
```

📌 `var` не робить C# динамічно типізованим.

---

### Константи
**Константа** — це змінна, значення якої не можна змінити.

```csharp
const double PI = 3.14;
```

---

### Імена змінних
Правила іменування:
- не можуть починатися з цифри
- не можуть містити пробіли
- використовують camelCase

```csharp
int userAge;
string firstName;
```

---

## 💻 Простий приклад

```csharp
using System;

class Program
{
    static void Main()
    {
        string language = "C#";
        int year = 2024;

        Console.WriteLine(language);
        Console.WriteLine(year);
    }
}
```

---

## 📝 Підсумки
- C# має строгий синтаксис
- Типи змінних вказуються явно
- `var` — це зручність, а не динаміка
- Константи захищають значення від змін

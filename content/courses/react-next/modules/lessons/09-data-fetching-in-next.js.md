# Data Fetching in Next.js

Next.js надає зручні способи **отримання даних з сервера**.
Завдяки цьому можна створювати швидкі, SEO-дружні та масштабовані додатки.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке data fetching
- знати основні способи отримання даних у Next.js
- розуміти різницю між SSR та SSG

---

## 🧠 Теорія

### Що таке data fetching
**Data fetching** — це процес отримання даних з:
- API
- бази даних
- зовнішніх сервісів

Ці дані використовуються для відображення контенту на сторінці.

---

### Основні підходи в Next.js
Next.js підтримує кілька способів отримання даних:

- Server-Side Rendering (SSR)
- Static Site Generation (SSG)
- Client-Side Fetching

Кожен підхід має свої переваги.

---

### Server-Side Rendering (SSR)
При SSR сторінка генерується **на сервері при кожному запиті**.

```js
export async function getServerSideProps() {
  const res = await fetch("https://api.example.com/posts");
  const data = await res.json();

  return { props: { data } };
}
```

📌 Підходить для даних, які часто змінюються.

---

### Static Site Generation (SSG)
При SSG сторінка генерується **один раз під час білду**.

```js
export async function getStaticProps() {
  const res = await fetch("https://api.example.com/posts");
  const data = await res.json();

  return { props: { data } };
}
```

📌 Дуже швидко працює та добре для SEO.

---

### Client-Side Fetching
Дані можна отримувати і на клієнті, використовуючи `useEffect`.

```jsx
useEffect(() => {
  fetch("/api/data")
    .then(res => res.json())
    .then(data => setData(data));
}, []);
```

📌 Часто використовується для динамічних частин UI.

---

### Коли що використовувати
- **SSR** — актуальні дані
- **SSG** — статичний контент
- **Client-side** — інтерактивні елементи

---

## 💻 Простий приклад

```jsx
function Posts({ posts }) {
  return (
    <ul>
      {posts.map(post => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}
```

---

## 📝 Підсумки
- Data fetching — основа динамічних сторінок
- Next.js підтримує SSR та SSG
- Різні підходи — для різних задач
- Правильний вибір впливає на продуктивність

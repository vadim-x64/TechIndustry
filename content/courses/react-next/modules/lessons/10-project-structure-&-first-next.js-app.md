# Project Structure & First Next.js App

Розуміння структури проєкту Next.js допомагає **швидко орієнтуватися в коді**
та правильно організовувати додаток з самого початку.

У цьому уроці ми розглянемо базову структуру Next.js та створення першого застосунку.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти структуру Next.js-проєкту
- знати призначення основних папок і файлів
- створити та запустити перший Next.js-додаток

---

## 🧠 Теорія

### Створення Next.js проєкту
Новий Next.js-проєкт створюється за допомогою команди:

```bash
npx create-next-app my-app
```

Після встановлення можна запустити проєкт:

```bash
npm run dev
```

📌 Додаток буде доступний за адресою `http://localhost:3000`.

---

### Основна структура проєкту
Типова структура Next.js виглядає так:

```
my-app/
 ├─ pages/
 ├─ public/
 ├─ styles/
 ├─ package.json
 └─ next.config.js
```

Кожна папка має свою роль.

---

### Папка pages
`pages` — це серце маршрутизації Next.js.

- `index.js` — головна сторінка
- кожен файл = окремий маршрут
- підтримує динамічні маршрути

```jsx
export default function Home() {
  return <h1>Hello Next.js</h1>;
}
```

---

### Папка public
`public` використовується для статичних файлів:

- зображення
- іконки
- шрифти

Доступ до файлів відбувається напряму:

```jsx
<img src="/logo.png" />
```

---

### Папка styles
`styles` містить CSS-файли.

Зазвичай тут є:
- `globals.css`
- CSS Modules для окремих компонентів

📌 Next.js підтримує CSS Modules з коробки.

---

### package.json
Файл `package.json` містить:
- залежності проєкту
- скрипти запуску
- інформацію про додаток

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start"
}
```

---

## 💻 Перший простий приклад

```jsx
export default function App() {
  return (
    <main>
      <h1>My first Next.js app</h1>
      <p>Welcome!</p>
    </main>
  );
}
```

Цей код створює просту сторінку без додаткових налаштувань.

---

## 📝 Підсумки
- Next.js має зрозумілу структуру
- `pages` відповідає за маршрути
- `public` — для статичних файлів
- Правильна структура полегшує масштабування

🎉 **Вітаємо! Ти завершив теоретичний курс React & Next.js**

# JSX and Component Basics

JSX — це спеціальний синтаксис, який дозволяє писати HTML-подібний код
безпосередньо всередині JavaScript. Саме з JSX та компонентів починається робота з React.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- зрозуміти, що таке JSX
- писати прості React-компоненти
- розрізняти компонент і звичайну функцію

---

## 🧠 Теорія

### Що таке JSX
**JSX (JavaScript XML)** — це розширення синтаксису JavaScript,
яке виглядає як HTML, але насправді є JavaScript-кодом.

Приклад JSX:
```jsx
<h1>Hello, React!</h1>
```

📌 Браузер не розуміє JSX напряму — він перетворюється на JavaScript під час збірки.

---

### JSX — це JavaScript
JSX всередині перетворюється на виклики `React.createElement`.

Тобто цей код:
```jsx
<h1>Hello</h1>
```

Перетворюється приблизно на:
```js
React.createElement("h1", null, "Hello");
```

👉 JSX робить код **читабельнішим і зрозумілішим**.

---

### Правила JSX
JSX має кілька важливих правил:

- компонент повинен повертати **один кореневий елемент**
- HTML-атрибути пишуться у стилі `camelCase`
- JavaScript вставляється у `{}`

Приклад:
```jsx
<h1 className="title">{2 + 2}</h1>
```

---

### Що таке компонент
**Компонент** — це функція, яка повертає JSX.

```jsx
function Header() {
  return <h1>Header</h1>;
}
```

Компонент:
- починається з великої літери
- може використовуватися як HTML-тег

---

### Використання компонентів
Компоненти можна вкладати один в один.

```jsx
function App() {
  return (
    <div>
      <Header />
      <p>Main content</p>
    </div>
  );
}
```

📌 Це дозволяє будувати інтерфейс з маленьких частин.

---

### Чому компоненти важливі
Компоненти:
- спрощують структуру коду
- дозволяють повторне використання
- роблять проєкт масштабованим

Без компонентів великі React-додатки було б складно підтримувати.

---

## 💻 Простий приклад

```jsx
function Button() {
  return <button>Click me</button>;
}
```

Цей компонент можна використовувати багато разів.

---

## 📝 Підсумки
- JSX — це синтаксис для опису UI
- JSX виглядає як HTML, але це JavaScript
- Компонент — це функція, що повертає JSX
- React-додаток складається з компонентів

# Routing in Next.js

Маршрутизація в Next.js дозволяє **створювати сторінки без додаткових бібліотек**.
Вона побудована на файловій структурі, що робить навігацію простою та зрозумілою.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, як працює routing у Next.js
- створювати сторінки за допомогою файлів
- використовувати навігацію між сторінками

---

## 🧠 Теорія

### Що таке routing
**Routing** — це механізм, який визначає,
який контент показувати користувачу за певною URL-адресою.

У Next.js routing працює **автоматично** на основі структури папок.

---

### Файлова маршрутизація
У Next.js кожен файл у папці `pages` або `app`
стає окремою сторінкою сайту.

Приклад:
```
pages/index.js   →  /
pages/about.js   →  /about
pages/blog.js    →  /blog
```

📌 Назва файлу = шлях у браузері.

---

### Головна сторінка
Файл `index.js` завжди відповідає за головну сторінку.

```jsx
export default function Home() {
  return <h1>Home page</h1>;
}
```

---

### Динамічні маршрути
Next.js підтримує динамічні сторінки.

```js
pages/posts/[id].js
```

Такий файл відповідає маршрутам:
```
/posts/1
/posts/2
```

---

### Навігація між сторінками
Для переходів між сторінками використовується компонент `Link`.

```jsx
import Link from "next/link";

<Link href="/about">About</Link>
```

📌 Це дозволяє Next.js робити швидку навігацію без перезавантаження сторінки.

---

### useRouter
Хук `useRouter` дає доступ до поточного маршруту.

```jsx
import { useRouter } from "next/router";

const router = useRouter();
console.log(router.query);
```

Його часто використовують для динамічних сторінок.

---

## 💻 Простий приклад

```jsx
function BlogPost() {
  const router = useRouter();

  return <p>Post ID: {router.query.id}</p>;
}
```

---

## 📝 Підсумки
- Next.js використовує файлову маршрутизацію
- Кожен файл — окрема сторінка
- Динамічні маршрути створюються через `[param]`
- `Link` і `useRouter` керують навігацією

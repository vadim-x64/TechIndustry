# Props and Component Reusability

Props — це спосіб **передачі даних у компоненти React**.
Вони дозволяють робити компоненти гнучкими та багаторазово використовуваними.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке props
- передавати дані в компоненти
- створювати повторно використовувані компоненти

---

## 🧠 Теорія

### Що таке props
**Props (properties)** — це обʼєкт з даними, який передається компоненту ззовні.

Простими словами:
> props — це параметри для React-компонента

---

### Передача props у компонент
Props передаються так само, як атрибути HTML.

```jsx
<User name="Alex" age={25} />
```

Компонент приймає props як аргумент функції:

```jsx
function User(props) {
  return <p>{props.name}, {props.age}</p>;
}
```

---

### Використання props
Props можна використовувати:
- у JSX
- у логіці компонента
- для налаштування вигляду та поведінки

```jsx
function Button(props) {
  return <button>{props.text}</button>;
}
```

---

### Деструктуризація props
Щоб код був чистішим, props часто деструктуризують.

```jsx
function Button({ text }) {
  return <button>{text}</button>;
}
```

📌 Це найпоширеніший підхід у React.

---

### Props — тільки для читання
Важливе правило:
> ❌ props не можна змінювати всередині компонента

Props передаються **від батьківського компонента до дочірнього**.

---

### Повторне використання компонентів
Завдяки props один і той самий компонент можна використовувати багато разів.

```jsx
<Button text="Save" />
<Button text="Delete" />
<Button text="Cancel" />
```

📌 Компонент один — вигляд і поведінка різні.

---

### Чому це важливо
Props дозволяють:
- уникати дублювання коду
- створювати універсальні компоненти
- легко масштабувати додаток

---

## 💻 Простий приклад

```jsx
function Card({ title, description }) {
  return (
    <div>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
}
```

---

## 📝 Підсумки
- Props передають дані у компоненти
- Вони роблять компоненти гнучкими
- Props не можна змінювати
- Повторне використання — ключова перевага React

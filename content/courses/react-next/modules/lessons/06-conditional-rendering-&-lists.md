# Conditional Rendering & Lists

Conditional Rendering дозволяє **показувати різний інтерфейс**
залежно від стану або умов.
Lists використовуються для відображення колекцій даних у React.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- використовувати умовний рендеринг
- працювати зі списками в React
- правильно застосовувати `key`

---

## 🧠 Теорія

### Що таке conditional rendering
**Conditional rendering** — це підхід, коли React
відображає різний JSX залежно від умови.

Простими словами:
> інтерфейс змінюється залежно від даних

---

### Умови з if
Найпростіший спосіб — використання `if`.

```jsx
if (!isLoggedIn) {
  return <p>Please log in</p>;
}
```

Якщо умова не виконується — компонент може показувати інший UI.

---

### Умови з тернарним оператором
Часто використовується тернарний оператор.

```jsx
{isLoggedIn ? <Dashboard /> : <Login />}
```

📌 Це найпопулярніший спосіб умовного рендерингу в JSX.

---

### Логічний оператор &&
Використовується, коли потрібно щось показати **лише за умови true**.

```jsx
{isAdmin && <AdminPanel />}
```

Якщо `isAdmin` — `false`, нічого не відобразиться.

---

### Що таке списки (lists)
Списки дозволяють рендерити масиви даних.

```jsx
const numbers = [1, 2, 3];
```

У React списки зазвичай створюються за допомогою `map`.

---

### Рендеринг списків
```jsx
<ul>
  {numbers.map((num) => (
    <li>{num}</li>
  ))}
</ul>
```

📌 Кожен елемент списку має бути JSX-елементом.

---

### Властивість key
При роботі зі списками **обовʼязково потрібен `key`**.

```jsx
<li key={num}>{num}</li>
```

`key` допомагає React:
- ефективно оновлювати список
- уникати помилок рендерингу

---

### Типові помилки
- використання індексу масиву як `key`
- відсутність `key`
- складні умови прямо в JSX

---

## 💻 Простий приклад

```jsx
function UserList({ users }) {
  return (
    <ul>
      {users.map((user) => (
        <li key={user.id}>
          {user.name}
        </li>
      ))}
    </ul>
  );
}
```

---

## 📝 Підсумки
- Conditional rendering керує відображенням UI
- Тернарний оператор — найзручніший спосіб
- Lists створюються через `map`
- `key` є обовʼязковим для списків

# Handling Events in React

Події в React дозволяють компонентам **реагувати на дії користувача**:
кліки, введення тексту, наведення миші та інші взаємодії.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- обробляти події в React
- працювати з event-обʼєктом
- поєднувати події зі state

---

## 🧠 Теорія

### Що таке події
**Подія (event)** — це дія користувача або браузера,
на яку може реагувати програма.

Приклади подій:
- клік миші
- введення тексту
- відправка форми

---

### Події в React
У React події схожі на HTML, але мають свої особливості:

- використовують `camelCase`
- передаються як функції
- працюють через SyntheticEvent

Приклад:
```jsx
<button onClick={handleClick}>Click</button>
```

---

### Обробник події
Обробник — це функція, яка виконується при події.

```jsx
function handleClick() {
  console.log("Button clicked");
}
```

📌 Функцію **не потрібно викликати**, лише передавати посилання.

---

### Події та state
Події часто використовуються для зміни state.

```jsx
<button onClick={() => setCount(count + 1)}>
  Increase
</button>
```

Коли користувач клікає:
1. спрацьовує подія
2. змінюється state
3. оновлюється інтерфейс

---

### Event object
React передає обʼєкт події в обробник.

```jsx
function handleChange(event) {
  console.log(event.target.value);
}
```

📌 Це часто використовується у формах.

---

### Обробка подій у формах
```jsx
<input
  type="text"
  onChange={handleChange}
/>
```

Таким чином React контролює введення користувача.

---

## 💻 Простий приклад

```jsx
function ClickCounter() {
  const [count, setCount] = useState(0);

  function handleClick() {
    setCount(count + 1);
  }

  return (
    <div>
      <p>{count}</p>
      <button onClick={handleClick}>
        Click me
      </button>
    </div>
  );
}
```

---

## 📝 Підсумки
- Події реагують на дії користувача
- Обробники — це функції
- Події тісно повʼязані зі state
- React використовує camelCase для подій

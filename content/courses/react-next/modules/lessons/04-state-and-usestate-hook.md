# State and useState Hook

State — це внутрішній стан компонента React.
Саме state дозволяє компонентам **змінюватися та реагувати на дії користувача**.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке state
- використовувати хук `useState`
- оновлювати стан компонента правильно

---

## 🧠 Теорія

### Що таке state
**State** — це дані, які належать конкретному компоненту
і можуть змінюватися з часом.

Простими словами:
> state — це памʼять компонента

Коли state змінюється — React **перемальовує інтерфейс**.

---

### Чому не props
На відміну від props:
- props приходять ззовні
- state керується самим компонентом

📌 State використовується для динамічних даних.

---

### Хук useState
Для роботи зі state у функціональних компонентах
React надає хук `useState`.

```jsx
import { useState } from "react";
```

---

### Створення state
```jsx
const [count, setCount] = useState(0);
```

Тут:
- `count` — поточне значення state
- `setCount` — функція для оновлення
- `0` — початкове значення

---

### Оновлення state
State оновлюється **тільки через setter-функцію**.

```jsx
setCount(count + 1);
```

📌 Прямо змінювати state не можна.

---

### Перерендер компонента
Коли викликається `setState`:
1. React оновлює state
2. Компонент перерендерюється
3. UI показує нові дані

Це відбувається автоматично.

---

### Кілька state
В одному компоненті може бути кілька state.

```jsx
const [name, setName] = useState("");
const [age, setAge] = useState(0);
```

---

## 💻 Простий приклад

```jsx
function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>{count}</p>
      <button onClick={() => setCount(count + 1)}>
        Increase
      </button>
    </div>
  );
}
```

Цей компонент:
- зберігає стан
- реагує на клік
- оновлює інтерфейс

---

## 📝 Підсумки
- State зберігає дані компонента
- `useState` — основний хук для стану
- State оновлюється через setter
- Зміна state викликає перерендер

# What is React & Why React

React — це популярна JavaScript-бібліотека для створення **користувацьких інтерфейсів**.
Вона допомагає будувати швидкі, масштабовані та зручні вебзастосунки.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- пояснити, що таке React простими словами
- зрозуміти, навіщо використовують React
- знати, де і коли React підходить найкраще

---

## 🧠 Теорія

### Що таке React
**React** — це бібліотека JavaScript для побудови інтерфейсу користувача (UI).

Основна ідея React:
> інтерфейс — це результат стану (state)

React дозволяє:
- розбивати UI на компоненти
- керувати станом додатка
- ефективно оновлювати сторінку

---

### Компонентний підхід
React побудований на **компонентах**.

Компонент — це:
- незалежна частина інтерфейсу
- повторно використовуваний елемент
- поєднання логіки та розмітки

Приклад компонентів:
- кнопка
- форма
- картка товару
- навігаційне меню

---

### Чому не звичайний JavaScript
У великих проєктах робота з чистим JavaScript стає складною:

- важко підтримувати код
- складно керувати станом
- багато ручних оновлень DOM

React вирішує ці проблеми:
- автоматично оновлює інтерфейс
- спрощує логіку
- робить код структурованим

---

### Virtual DOM
React використовує **Virtual DOM** — віртуальне представлення сторінки.

Як це працює:
1. Змінюється стан
2. React оновлює Virtual DOM
3. Порівнює його з попереднім
4. Оновлює тільки потрібні елементи в реальному DOM

📌 Це робить додатки швидкими.

---

### Де використовується React
React широко застосовується у сучасній веброзробці:

- односторінкові додатки (SPA)
- складні інтерфейси
- дашборди
- комерційні вебплатформи

React використовують тисячі компаній по всьому світу.

---

## 💻 Простий приклад

```js
function App() {
  return <h1>Hello, React!</h1>;
}
```

Цей код описує інтерфейс як функцію.

---

## 📝 Підсумки
- React — бібліотека для створення UI
- Він базується на компонентах
- Virtual DOM підвищує продуктивність
- React спрощує розробку великих додатків

# useEffect and Component Lifecycle

Хук `useEffect` дозволяє виконувати побічні ефекти в React-компонентах.
Він замінює життєвий цикл компонентів, який раніше використовувався у класах.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке побічні ефекти
- використовувати хук `useEffect`
- знати, як працює життєвий цикл компонента

---

## 🧠 Теорія

### Що таке побічний ефект
**Побічний ефект (side effect)** — це дія, яка виходить за межі рендерингу UI.

Приклади:
- запит до API
- робота з таймерами
- підписка на події
- зміна `document.title`

---

### Хук useEffect
`useEffect` дозволяє виконувати такі дії після рендеру компонента.

```jsx
import { useEffect } from "react";
```

---

### Базове використання
```jsx
useEffect(() => {
  console.log("Component rendered");
});
```

📌 Цей ефект виконується **після кожного рендеру**.

---

### Масив залежностей
Другий аргумент `useEffect` — це **масив залежностей**.

```jsx
useEffect(() => {
  console.log("Only once");
}, []);
```

Тут ефект виконається:
- лише один раз
- після першого рендеру компонента

---

### useEffect зі state
```jsx
useEffect(() => {
  console.log("Count changed");
}, [count]);
```

Ефект виконається щоразу, коли зміниться `count`.

---

### Очищення ефектів
`useEffect` може повертати функцію очищення.

```jsx
useEffect(() => {
  const timer = setInterval(() => {
    console.log("Tick");
  }, 1000);

  return () => clearInterval(timer);
}, []);
```

📌 Це важливо для уникнення витоків памʼяті.

---

### Життєвий цикл компонента
У функціональних компонентах:
- mount → `useEffect(..., [])`
- update → `useEffect(..., [deps])`
- unmount → cleanup function

---

## 💻 Простий приклад

```jsx
function TitleUpdater() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    document.title = `Count: ${count}`;
  }, [count]);

  return (
    <button onClick={() => setCount(count + 1)}>
      Increase
    </button>
  );
}
```

---

## 📝 Підсумки
- `useEffect` керує побічними ефектами
- Масив залежностей контролює виконання
- Cleanup потрібен для безпеки
- `useEffect` замінює lifecycle методи

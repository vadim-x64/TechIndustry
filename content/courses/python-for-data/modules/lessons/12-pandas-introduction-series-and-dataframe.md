# Pandas Introduction: Series & DataFrame

Pandas — ключова бібліотека для **табличного аналізу даних** у Python.
Вона робить роботу з даними схожою на роботу з таблицями Excel або базами даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке Pandas
- працювати з Series та DataFrame
- виконувати базові операції з табличними даними

---

## 🧠 Теорія

### Що таке Pandas
**Pandas** — це бібліотека для обробки та аналізу структурованих даних.

Вона дозволяє:
- читати дані з файлів
- очищати та змінювати дані
- аналізувати таблиці

📌 Pandas побудований на основі NumPy.

---

### Імпорт Pandas
```python
import pandas as pd
```

Це стандартний спосіб імпорту бібліотеки.

---

### Series
**Series** — це одновимірна структура даних,
схожа на список з індексами.

```python
import pandas as pd

s = pd.Series([10, 20, 30])
print(s)
```

---

### Індекси в Series
Кожне значення має індекс.

```python
s[0]
```

Індекси дозволяють швидко доступатися до даних.

---

### DataFrame
**DataFrame** — це двовимірна таблиця з рядками та стовпцями.

```python
data = {
    "name": ["Anna", "Oleh"],
    "age": [25, 30]
}

df = pd.DataFrame(data)
print(df)
```

---

### Доступ до даних
```python
df["name"]
```

Або по рядках:
```python
df.loc[0]
```

---

### Основні методи DataFrame
```python
df.head()
df.info()
df.describe()
```

Ці методи дають швидкий огляд даних.

---

## 💻 Простий приклад

```python
import pandas as pd

data = {
    "score": [80, 90, 85]
}

df = pd.DataFrame(data)
print(df.mean())
```

---

## 📝 Підсумки
- Pandas — основний інструмент data analysis
- Series — одновимірні дані
- DataFrame — табличні дані
- Pandas спрощує роботу з великими наборами даних

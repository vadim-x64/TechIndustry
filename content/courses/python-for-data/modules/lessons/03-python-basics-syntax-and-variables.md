# Python Basics: Syntax & Variables

Python відомий простим і читабельним синтаксисом.
Саме з базового синтаксису та змінних починається будь-яка робота з Python,
зокрема й аналіз даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти базовий синтаксис Python
- створювати та використовувати змінні
- писати прості Python-програми

---

## 🧠 Теорія

### Синтаксис Python
Синтаксис — це правила написання коду.
Python відрізняється від інших мов тим, що використовує **відступи** замість фігурних дужок.

Приклад:
```python
if True:
    print("Hello, Python")
```

📌 Відступи в Python обовʼязкові та мають значення.

---

### Коментарі
Коментарі використовуються для пояснення коду
і не виконуються програмою.

```python
# Це коментар
print("Code runs")
```

Коментарі роблять код зрозумілішим.

---

### Змінні
**Змінна** — це імʼя, яке зберігає значення.

```python
age = 25
name = "Anna"
```

Python автоматично визначає тип даних змінної.

---

### Імена змінних
Правила іменування:
- використовуються літери, цифри та `_`
- імʼя не може починатися з цифри
- не можна використовувати зарезервовані слова

Приклад:
```python
user_age = 30
```

---

### Динамічна типізація
Python має **динамічну типізацію** —
тип змінної може змінюватися під час виконання.

```python
value = 10
value = "text"
```

📌 Це спрощує код, але вимагає уважності.

---

### Присвоєння кількох змінних
```python
x, y, z = 1, 2, 3
```

Це зручний спосіб працювати з кількома значеннями.

---

## 💻 Простий приклад

```python
name = "Python"
year = 2024

print(name)
print(year)
```

Цей код виводить значення змінних у консоль.

---

## 📝 Підсумки
- Python має простий синтаксис
- Відступи визначають блоки коду
- Змінні зберігають дані
- Типи даних визначаються автоматично

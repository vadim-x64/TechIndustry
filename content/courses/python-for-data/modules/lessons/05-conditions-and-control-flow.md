# Conditions and Control Flow

Умови та керування потоком виконання дозволяють програмі
**приймати рішення та виконувати різний код залежно від ситуації**.
Це фундаментальна частина будь-якої Python-програми.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- використовувати умовні оператори
- писати логіку з `if`, `elif`, `else`
- керувати потоком виконання програми

---

## 🧠 Теорія

### Що таке умова
**Умова** — це вираз, який повертає `True` або `False`.

```python
age >= 18
```

На основі результату Python вирішує, який код виконувати.

---

### Оператор if
`if` виконує код, якщо умова істинна.

```python
age = 20

if age >= 18:
    print("Access granted")
```

📌 Якщо умова `False`, код усередині блоку не виконається.

---

### else
`else` виконується, якщо умова в `if` хибна.

```python
if age >= 18:
    print("Access granted")
else:
    print("Access denied")
```

---

### elif
`elif` використовується для перевірки кількох умов.

```python
score = 85

if score >= 90:
    grade = "A"
elif score >= 75:
    grade = "B"
else:
    grade = "C"
```

📌 Перевірка йде зверху вниз.

---

### Оператори порівняння
Найчастіше використовуються:

- `==` дорівнює
- `!=` не дорівнює
- `>` `<`
- `>=` `<=`

```python
x = 10
print(x == 10)
```

---

### Логічні оператори
Дозволяють поєднувати умови:

- `and`
- `or`
- `not`

```python
if age >= 18 and has_id:
    print("Allowed")
```

---

## 💻 Простий приклад

```python
temperature = 30

if temperature > 25:
    print("Hot")
else:
    print("Normal")
```

---

## 📝 Підсумки
- Умови керують логікою програми
- `if / elif / else` — основа control flow
- Оператори порівняння та логіки працюють разом
- Відступи визначають блоки коду

# Loops and Iterations

Цикли дозволяють **виконувати один і той самий код багато разів**.
В аналізі даних цикли часто використовуються для обробки списків,
файлів та наборів значень.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, навіщо потрібні цикли
- використовувати `for` та `while`
- керувати виконанням циклів

---

## 🧠 Теорія

### Що таке цикл
**Цикл** — це конструкція, яка повторює виконання коду,
поки виконується певна умова.

Цикли допомагають уникати дублювання коду.

---

### Цикл for
Цикл `for` використовується для перебору колекцій даних.

```python
numbers = [1, 2, 3]

for n in numbers:
    print(n)
```

📌 `for` — найпопулярніший цикл у Python.

---

### Функція range
`range()` створює послідовність чисел.

```python
for i in range(5):
    print(i)
```

Цей код виведе числа від 0 до 4.

---

### Цикл while
`while` виконується, поки умова істинна.

```python
count = 0

while count < 3:
    print(count)
    count += 1
```

📌 Важливо слідкувати, щоб умова колись стала `False`.

---

### break та continue
Ці оператори керують виконанням циклу.

- `break` — зупиняє цикл
- `continue` — пропускає поточну ітерацію

```python
for i in range(5):
    if i == 3:
        break
    print(i)
```

---

### Вкладені цикли
Цикли можна вкладати один в один.

```python
for i in range(2):
    for j in range(2):
        print(i, j)
```

📌 Вкладені цикли часто використовуються при роботі з таблицями даних.

---

## 💻 Простий приклад

```python
total = 0

for i in range(1, 6):
    total += i

print(total)
```

Цей код обчислює суму чисел від 1 до 5.

---

## 📝 Підсумки
- Цикли повторюють виконання коду
- `for` використовується для колекцій
- `while` залежить від умови
- `break` та `continue` керують циклом

# Functions and Built-in Methods

Функції дозволяють **групувати код і використовувати його багаторазово**.
Вбудовані методи Python допомагають працювати з даними швидше та ефективніше,
що особливо важливо в аналізі даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- створювати власні функції
- використовувати параметри та `return`
- працювати з вбудованими функціями Python

---

## 🧠 Теорія

### Що таке функція
**Функція** — це блок коду, який виконує певну задачу
і може бути викликаний у будь-якому місці програми.

```python
def greet():
    print("Hello")
```

---

### Виклик функції
Щоб функція виконалась, її потрібно викликати.

```python
greet()
```

📌 Функцію можна викликати багато разів.

---

### Параметри функції
Функції можуть приймати дані — параметри.

```python
def greet(name):
    print("Hello", name)
```

---

### Повернення значення
Функція може повертати результат за допомогою `return`.

```python
def add(a, b):
    return a + b
```

📌 Після `return` функція завершує роботу.

---

### Вбудовані функції
Python має багато корисних вбудованих функцій:

- `len()`
- `sum()`
- `max()`
- `min()`
- `type()`

```python
numbers = [1, 2, 3]
print(len(numbers))
```

---

### Методи рядків і списків
Рядки та списки мають власні методи.

```python
text = "data analysis"
print(text.upper())
```

```python
items = [1, 2, 3]
items.append(4)
```

---

## 💻 Простий приклад

```python
def average(numbers):
    return sum(numbers) / len(numbers)

data = [10, 20, 30]
print(average(data))
```

---

## 📝 Підсумки
- Функції структурують код
- Параметри передають дані
- `return` повертає результат
- Вбудовані методи спрощують аналіз даних

# Working with Files (CSV, TXT)

Файли — один з основних джерел даних у аналізі.
Python дозволяє **читати, записувати та обробляти файли** різних форматів,
зокрема TXT та CSV.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- відкривати та читати текстові файли
- записувати дані у файли
- працювати з CSV-файлами у Python

---

## 🧠 Теорія

### Робота з файлами в Python
Для роботи з файлами використовується функція `open()`.

```python
file = open("data.txt", "r")
```

Режими відкриття файлу:
- `r` — читання
- `w` — запис
- `a` — додавання

---

### Читання TXT-файлів
```python
with open("data.txt", "r") as file:
    content = file.read()
    print(content)
```

📌 Конструкція `with` автоматично закриває файл.

---

### Читання по рядках
```python
with open("data.txt", "r") as file:
    for line in file:
        print(line)
```

Це зручно для великих файлів.

---

### Запис у файл
```python
with open("output.txt", "w") as file:
    file.write("Hello Data")
```

📌 Режим `w` перезаписує файл.

---

### Робота з CSV-файлами
CSV — найпопулярніший формат для табличних даних.

```python
import csv

with open("data.csv", "r") as file:
    reader = csv.reader(file)
    for row in reader:
        print(row)
```

---

### Коли використовуються CSV
CSV-файли часто застосовуються для:
- експорту даних
- обміну між системами
- підготовки даних для аналізу

---

## 💻 Простий приклад

```python
import csv

with open("scores.csv", "r") as file:
    reader = csv.reader(file)
    for name, score in reader:
        print(name, score)
```

---

## 📝 Підсумки
- Python легко працює з файлами
- TXT підходить для простих даних
- CSV — стандарт для табличних даних
- `with open()` — безпечний підхід

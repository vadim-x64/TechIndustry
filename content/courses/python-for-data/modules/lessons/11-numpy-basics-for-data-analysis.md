# NumPy Basics for Data Analysis

NumPy — це фундаментальна бібліотека для **числових обчислень та аналізу даних** у Python.
Вона дозволяє працювати з масивами даних швидко та ефективно.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке NumPy
- створювати та використовувати NumPy-масиви
- виконувати базові операції над даними

---

## 🧠 Теорія

### Що таке NumPy
**NumPy (Numerical Python)** — це бібліотека для роботи з багатовимірними масивами
та математичними операціями над ними.

Переваги NumPy:
- висока швидкість
- зручна робота з числами
- основа для Pandas, SciPy та інших бібліотек

---

### Встановлення та імпорт
NumPy встановлюється через pip:

```bash
pip install numpy
```

Імпорт бібліотеки:

```python
import numpy as np
```

---

### NumPy-масиви (ndarray)
Основна структура даних у NumPy — **ndarray**.

```python
import numpy as np

arr = np.array([1, 2, 3, 4])
print(arr)
```

📌 NumPy-масиви зберігають дані одного типу.

---

### Операції з масивами
NumPy дозволяє виконувати операції над усім масивом одразу.

```python
arr = np.array([1, 2, 3])
print(arr * 2)
```

Це називається **векторизація**.

---

### Базові математичні функції
```python
np.sum(arr)
np.mean(arr)
np.max(arr)
np.min(arr)
```

Ці функції часто використовуються в аналізі даних.

---

### Створення масивів
```python
np.zeros(5)
np.ones(5)
np.arange(0, 10, 2)
```

Вони допомагають швидко створювати тестові дані.

---

## 💻 Простий приклад

```python
import numpy as np

data = np.array([10, 20, 30, 40])
average = np.mean(data)

print(average)
```

---

## 📝 Підсумки
- NumPy — основа data analysis у Python
- ndarray — головна структура даних
- Векторизація робить код швидким
- NumPy має багато корисних математичних функцій

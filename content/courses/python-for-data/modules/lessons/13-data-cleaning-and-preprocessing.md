# Data Cleaning and Preprocessing

Очищення та підготовка даних — один з найважливіших етапів аналізу даних.
Якість результатів напряму залежить від **якості вхідних даних**.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, що таке data cleaning
- працювати з пропущеними значеннями
- підготовлювати дані до аналізу

---

## 🧠 Теорія

### Що таке data cleaning
**Data cleaning** — це процес виправлення або видалення некоректних,
неповних чи зайвих даних.

Типові проблеми даних:
- пропущені значення
- дублікати
- неправильні типи даних
- зайві стовпці

---

### Пропущені значення
У Pandas пропущені значення зазвичай позначаються як `NaN`.

```python
df.isna()
```

Цей метод показує, де є пропуски.

---

### Видалення пропущених даних
```python
df.dropna()
```

📌 Використовується, коли пропущених значень небагато.

---

### Заповнення пропусків
Пропуски можна заповнювати середнім, медіаною або значенням за замовчуванням.

```python
df.fillna(0)
```

Або:
```python
df["age"].fillna(df["age"].mean())
```

---

### Видалення дублікатів
```python
df.duplicated()
df.drop_duplicates()
```

📌 Дублікати можуть спотворювати результати аналізу.

---

### Зміна типів даних
```python
df["age"] = df["age"].astype(int)
```

Це важливо для коректних обчислень.

---

### Перейменування стовпців
```python
df.rename(columns={"old": "new"})
```

Зрозумілі назви полегшують роботу з даними.

---

## 💻 Простий приклад

```python
import pandas as pd

data = {
    "age": [20, None, 30],
    "score": [80, 80, 90]
}

df = pd.DataFrame(data)
df["age"] = df["age"].fillna(df["age"].mean())
print(df)
```

---

## 📝 Підсумки
- Data cleaning — обовʼязковий етап аналізу
- Пропущені значення потрібно обробляти
- Дублікати слід видаляти
- Підготовлені дані = точніші результати

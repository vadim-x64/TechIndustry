# Data Types & Type Conversion

Типи даних визначають, **яку саме інформацію зберігає змінна**
та які операції можна з нею виконувати.
Для аналізу даних розуміння типів є особливо важливим.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- знати основні типи даних у Python
- перевіряти тип змінної
- виконувати приведення типів

---

## 🧠 Теорія

### Основні типи даних
Python має кілька базових типів даних:

- `int` — цілі числа
- `float` — дробові числа
- `str` — рядки
- `bool` — логічні значення

Ці типи використовуються найчастіше.

---

### Числові типи
```python
a = 10        # int
b = 3.14      # float
```

Python автоматично визначає тип числа.

📌 Дробові числа завжди мають тип `float`.

---

### Рядки (str)
Рядок — це послідовність символів.

```python
name = "Data Science"
```

Рядки можна створювати з:
- подвійних лапок
- одинарних лапок

---

### Логічний тип (bool)
Тип `bool` має лише два значення:

```python
True
False
```

Використовується в умовах та перевірках.

---

### Перевірка типу
Щоб дізнатися тип змінної, використовується функція `type()`.

```python
x = 5
print(type(x))
```

---

### Приведення типів
Іноді потрібно явно змінити тип даних.

```python
age = "25"
age = int(age)
```

Основні функції:
- `int()`
- `float()`
- `str()`
- `bool()`

---

### Навіщо це потрібно
У data analysis дані часто приходять:
- з файлів
- з API
- з баз даних

І не завжди мають правильний тип.

---

## 💻 Простий приклад

```python
price = "99.9"
price_float = float(price)

total = price_float * 2
print(total)
```

Цей код перетворює рядок у число та виконує обчислення.

---

## 📝 Підсумки
- Типи даних визначають поведінку змінних
- Python має динамічну типізацію
- `type()` показує тип змінної
- Приведення типів часто використовується в аналізі даних


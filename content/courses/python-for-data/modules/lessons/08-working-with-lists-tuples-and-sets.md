# Working with Lists, Tuples & Sets

Колекції даних — основа аналізу даних у Python.
Списки, кортежі та множини дозволяють **зберігати, обробляти та аналізувати набори значень**.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розрізняти списки, кортежі та множини
- виконувати базові операції з колекціями
- обирати правильний тип колекції для задачі

---

## 🧠 Теорія

### Списки (list)
**Список** — це впорядкована та змінювана колекція.

```python
numbers = [1, 2, 3, 4]
```

Особливості списків:
- можна змінювати
- підтримують індексацію
- часто використовуються в data analysis

---

### Операції зі списками
```python
numbers.append(5)
numbers.remove(2)
```

Також доступні:
- `len()`
- `sum()`
- `max()`
- `min()`

---

### Кортежі (tuple)
**Кортеж** — це впорядкована, але незмінювана колекція.

```python
point = (10, 20)
```

📌 Кортежі використовуються, коли дані не повинні змінюватися.

---

### Множини (set)
**Множина** — це невпорядкована колекція унікальних значень.

```python
values = {1, 2, 3, 3}
```

Результат:
```
{1, 2, 3}
```

📌 Множини корисні для видалення дублікатів.

---

### Основні операції з множинами
```python
a = {1, 2, 3}
b = {3, 4, 5}

print(a.union(b))
print(a.intersection(b))
```

---

### Коли що використовувати
- `list` — змінювані дані
- `tuple` — фіксовані дані
- `set` — унікальні значення

---

## 💻 Простий приклад

```python
data = [10, 20, 20, 30]

unique_data = set(data)
print(unique_data)
```

---

## 📝 Підсумки
- Списки — основна структура даних
- Кортежі захищають дані від змін
- Множини прибирають дублікати
- Правильний вибір структури спрощує аналіз

---

👉 Далі ми будемо вивчати:
- словники
- складні структури даних
- роботу з табличними даними 🚀

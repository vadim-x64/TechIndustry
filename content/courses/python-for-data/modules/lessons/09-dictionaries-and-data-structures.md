# Dictionaries and Data Structures

Словники — одна з найважливіших структур даних у Python.
Вони дозволяють **зберігати дані у форматі ключ–значення**,
що дуже зручно для аналізу реальних наборів даних.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- створювати та використовувати словники
- працювати з ключами та значеннями
- застосовувати словники для структурованих даних

---

## 🧠 Теорія

### Що таке словник
**Словник (dict)** — це колекція пар `ключ: значення`.

```python
user = {
    "name": "Anna",
    "age": 25
}
```

Ключі у словнику:
- унікальні
- незмінювані
- зазвичай рядки

---

### Доступ до значень
Отримати значення можна за ключем.

```python
print(user["name"])
```

Або безпечніше — через метод `get()`:

```python
print(user.get("email"))
```

---

### Зміна та додавання даних
Словники можна змінювати.

```python
user["age"] = 26
user["city"] = "Kyiv"
```

📌 Словники є змінюваними структурами.

---

### Видалення елементів
```python
del user["city"]
```

Або:

```python
user.pop("age")
```

---

### Перебір словника
Словники часто перебирають у циклах.

```python
for key, value in user.items():
    print(key, value)
```

---

### Словники в аналізі даних
Словники часто використовуються для:
- зберігання записів
- представлення JSON-даних
- підготовки даних до DataFrame

---

## 💻 Простий приклад

```python
scores = {
    "math": 90,
    "english": 85,
    "science": 92
}

average = sum(scores.values()) / len(scores)
print(average)
```

---

## 📝 Підсумки
- Словники зберігають дані у форматі ключ–значення
- Значення доступні через ключі
- Словники легко змінювати
- Вони часто використовуються в data analysis


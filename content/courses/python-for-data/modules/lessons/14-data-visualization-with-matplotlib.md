# Data Visualization with Matplotlib

Візуалізація даних допомагає **краще зрозуміти інформацію**
та побачити закономірності, які складно помітити у таблицях.
Matplotlib — базова бібліотека для побудови графіків у Python.

---

## 🎯 Мета уроку
Після цього уроку ти зможеш:
- розуміти, навіщо потрібна візуалізація даних
- будувати базові графіки з Matplotlib
- налаштовувати вигляд графіків

---

## 🧠 Теорія

### Що таке візуалізація даних
**Data visualization** — це представлення даних у вигляді графіків і діаграм.

Вона допомагає:
- швидко аналізувати дані
- знаходити тренди
- пояснювати результати іншим

---

### Бібліотека Matplotlib
**Matplotlib** — найпопулярніша бібліотека для візуалізації в Python.

Встановлення:
```bash
pip install matplotlib
```

Імпорт:
```python
import matplotlib.pyplot as plt
```

---

### Лінійний графік
Лінійні графіки часто використовуються для показу змін у часі.

```python
x = [1, 2, 3, 4]
y = [10, 20, 15, 25]

plt.plot(x, y)
plt.show()
```

---

### Назви та підписи
Графіки повинні бути зрозумілими.

```python
plt.title("Sales over time")
plt.xlabel("Day")
plt.ylabel("Sales")
```

---

### Точковий графік
```python
plt.scatter(x, y)
plt.show()
```

📌 Добре підходить для аналізу звʼязку між змінними.

---

### Стовпчикові діаграми
```python
plt.bar(x, y)
plt.show()
```

Часто використовуються для порівняння значень.

---

### Кілька графіків
```python
plt.plot(x, y)
plt.plot(x, [5, 15, 10, 20])
plt.show()
```

---

## 💻 Простий приклад

```python
import matplotlib.pyplot as plt

scores = [80, 90, 85, 95]

plt.plot(scores)
plt.title("Student scores")
plt.show()
```

---

## 📝 Підсумки
- Візуалізація робить дані зрозумілими
- Matplotlib — базова бібліотека для графіків
- Графіки допомагають знаходити тренди
- Гарний графік = зрозумілий аналіз
